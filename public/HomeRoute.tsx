// Home — account picker/header, the folder/list tree, fixed Owned/Wishlist/Bundles/Recent
// links (see docs/list-centric-redesign.md). Stub for now — built out in Phase 5 step 8, once
// accountsStore.ts/listsStore.ts have somewhere to render into and the routes it links to exist.
export default function HomeRoute() {
  return (
    <div class="route-placeholder">
      <h2>Home</h2>
      <p>Account picker, header, and the folder/list tree land here (implementation plan, Phase 5 step 8).</p>
    </div>
  );
}
