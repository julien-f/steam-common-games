# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added

- Frontend converted to TypeScript — every module under `public/` is now `.ts`, checked in strict mode by `npm run typecheck` (TypeScript 7 / tsgo, `tsconfig.json`). New `public/types.ts` holds the shared row/detail types (`Game`, `GameDetails`, …). Runtime behavior is unchanged; the existing Node test suite (still `.js`) imports the `.ts` modules directly via Node's native type stripping.
- `npm run dev:web` — a Vite dev server (`:5173`) with HMR for the TypeScript frontend, proxying `/api` to the Express backend. Development is now two processes (`dev:web` + `dev`); the production flow is unchanged (`npm run build` → `dist/`, served by `npm start`).
- Library Explorer: the side panel now shows an "In library" / "On wishlist" status badge right under the title for any game looked up via the search box (or opened from a table row/DLC link), regardless of which tab (Library/Wishlist) is currently active. `library.js` background-fetches whichever tab isn't currently loaded, purely to know appid membership, so both statuses are known even when only one tab's data is otherwise on screen.
- Test coverage for five new shared frontend modules (`test/tableViewPrefs.test.js`, `test/rowCache.test.js`, `test/panelNav.test.js`, `test/priceLoading.test.js`, `test/ownerListHtml.test.js`) — see Changed below.

### Fixed

- Side panel's Price card silently showed "No pricing data available" for every game on the comparison page, the Library Explorer's Library tab, and standalone lookups — `panel.js`'s `loadPrice` called `resolveRegion(getStoredRegion())` without importing either from `region.js`, throwing a `ReferenceError` that its own catch block swallowed into a plain "no data" result with no console error at all, so `POST /api/prices` never actually fired.
- Library Explorer's Wishlist tab: looking up a game via the search box that isn't already in the loaded wishlist showed no Price card at all — `loadWishlistPrices`' batch price fetch is keyed off `rowMap`, which a standalone-lookup game never enters. `openStandaloneLookup` now separately prices that one game (`fetchStandalonePrice`) when the Wishlist tab is active.
- A standalone lookup opened while on the Library tab still never priced itself after switching to the Wishlist tab — `openStandaloneLookup`'s wishlist-only price fetch only ran at open time, and `setActiveTab` only re-fetches when a player is loaded. `setActiveTab` now also prices the currently open standalone game on switching to the Wishlist tab if it hasn't been priced yet.

### Removed

- The `/vendor/data-table-core`, `/vendor/data-table-vanilla`, and `/vendor/hls.js` static routes (`server.js`) and the `<script type="importmap">` blocks in `index.html`/`library.html`/`bundles.html` that resolved bare npm specifiers against them. Both existed only for the pre-TypeScript `public/`-served-unbundled dev flow; Vite (`dev:web` and `build`) resolves these same npm dependencies itself now, and nothing else ever requested `/vendor/*`.

### Changed

- A fresh clone now requires `npm run build` before `npm start` will serve a working frontend — the Express static fallback serves `public/` raw, and browsers won't parse the TypeScript sources (previously plain JS). Use `npm run dev:web` for development instead.
- De-duplicated logic that `library.js`/`bundles.js` (and, for the owner list, `app.js`) each carried their own near-identical copy of, into shared modules:
  - `public/tableViewPrefs.js` — the `?lv=`/`?wv=`/`?bv=` table-view restore/persist/share/reset logic. Along the way, `bundles.js`'s copy is fixed to route its writes through `reorderUrlParams` the same way `library.js`'s always did (`urlState.js`'s `PARAM_ORDER` now also lists `bv`).
  - `public/rowCache.js` — the `@vates/data-table-vanilla` `setData()`-reference-identity fix (a row keeps its cached copy across renders until explicitly marked changed).
  - `public/panelNav.js` — the prev/next/random panel nav-bar rendering and the shared "step to the next/prev game in the list, with wraparound" logic used by both the nav buttons and lightbox arrow-key navigation.
  - `public/priceLoading.js` — the ITAD `/api/prices` response → row-fields mapping and the "failed request nulls the fields instead of leaving them on their loading placeholder forever" fallback.
  - `public/ownerListHtml.js` — the "Owned by" panel-card markup, sort, and playtime-meter rendering (`app.js`'s `buildOwnersHtml` and `library.js`'s `buildLibraryOwnersHtml` were byte-identical past the point where each page resolves its own owners array; only that resolution step stays page-local now).
  - `public/pageShell.js` — the `initNav` → `initLightbox` → `initPanel` bootstrap sequence all three pages repeat, fixing an incidental difference where `bundles.js` used to defer its `initNav` call to the end of its own async `init()` instead of calling it up front like the other two pages.

- Lightbox: a visible game-name caption in the toolbar (`.lb-caption`) — previously the game/shot identity only existed as invisible `alt`/`aria-label` text, so it was impossible to tell at a glance which game's media was showing.
- Side panel Price card: a compact line of historical lows (3mo/1yr/all-time, most recent first) under the buy line, whichever of the three ITAD already returned for that game — reuses `DEAL_RECORD_TIERS`' own icon/color/label (`public/utils.js`) so it stays in sync with the Best Deal badge above it. Consecutive tiers sharing the same amount (common when the all-time low happened recently) are collapsed into one entry with combined icons instead of repeating the price, and any low that equals the current best-deal price above (already badged there) is dropped entirely rather than shown twice.
- Test coverage for `region.js`, `accountsBar.js`, and `gameSearch.js` (`test/region.test.js`/`test/accountsBar.test.js`/`test/gameSearch.test.js`, 69 tests) — these had none despite backing real logic (region detection, the recent-searches/recent-games lists, `parseDirectAppid`), a gap the ES-module conversion above made easy to finally close since they're real `import`/`export` modules now rather than plain scripts reached only via the global scope. A minimal hand-rolled fake DOM (`hidden`/`innerHTML`/`addEventListener`/`closest`) covers the `render*`/`bind*` functions' event-delegation logic without a jsdom dependency. `initGameSearch` itself (the debounced fetch/keyboard-nav widget) is intentionally not covered — meaningful tests would need fetch/timer/full-element mocking disproportionate to its risk next to the pure logic it's built from, which is what's actually tested.

### Fixed

- ProtonDB: a game with a few reports but not enough for ProtonDB to confidently assign a tier (`tier: "pending"`) used to show as "no reports at all" everywhere in this app (side panel glance chip, Library Explorer/Bundles ProtonDB column) despite protondb.com itself showing those reports and its own provisional best-guess tier. `extractProtonDb` (`lib/steam.js`) now surfaces that provisional tier (flagged `pending: true`) instead of collapsing it to null; the side panel chip and table badge render it faded with a trailing "?" and a tooltip/caption noting it's provisional, rather than identically to a confirmed tier.
- Side panel Price card: three small inconsistencies with the table's own Best Deal cell/Price Status column, all sharing the same root cause — `dealRecordTier`'s tier data wasn't being read (or displayed) the same way in both places.
  - `discountPct` (the Discount/`bestDealCut` formula) moved from `gameColumns.js` to `public/utils.js`; `bundles.js`/`library.js` now import it from there directly (same as their other `utils.js` imports) instead of via `gameColumns.js`, and `panel.js`'s own price loader imports it too instead of hand-duplicating the formula inline.
  - The bold weight the table cell gives an all-time-low price (`rec.bold`) is now also applied in the panel's Price card, rather than being silently dropped there (previously masked by `.panel-price-amount`'s own unconditional bold CSS rule).
  - The historical-lows line's record badge (🔥/★/☆) now trails the price (`"$4.99 🔥"`), matching the convention used everywhere else (the main price line above it, `renderBestDeal`, `renderPriceStatus`) — it previously led the price (`"🔥 $4.99"`), the one place in the app doing it the other way round.

### Changed

- Performance: `hls.js` (~130KB gzipped — dwarfing the rest of this app's own JS combined) is now loaded via a dynamic `import()` inside `lightbox.js` the first time a non-Safari browser actually opens a video shot, instead of a static import shipped to every single page load regardless of whether that page's session ever touches video (most panel opens are screenshots only). Cut the `panel.js` production chunk from 436KB (134KB gzip) to 51KB (15KB gzip); Vite splits `hls.js` into its own chunk, fetched on demand. Memoized so a second video reuses the already-resolved module; the adjacent-shot prefetch (which already prefetches an upcoming video's HLS manifest) now also warms this same import ahead of time, so normal next/prev stepping through media doesn't pay the load latency the lazy-load would otherwise add. A per-`<video>`-element token guards against a slow-resolving import landing after the user already stepped away from that shot.
- Performance: `dist/assets/*` (Vite's content-hashed production build output) is now served with `Cache-Control: public, max-age=31536000, immutable` — those filenames are genuinely immutable once built, so a repeat visitor's browser can skip the conditional-GET round-trip entirely instead of re-validating an ETag for a file that can never change under that exact hashed name. The `dist/*.html` entry files (which reference the current hashes) and the `public/` fallback path (unhashed filenames, can't get this treatment) are unaffected.
- Adopted [Vite](https://vite.dev/) as a real build tool. `npm run build` (`vite.config.js`) bundles `public/`'s four pages into a hashed, optimized `dist/`; `server.js` serves `dist/` automatically once it exists, falling back to serving `public/` directly otherwise — so `npm start`/`npm run dev` keep working with zero build step for local dev, exactly as before. As part of this, the vendored `public/hls.min.js` (hls.js 1.5.13, manually copied in) was replaced with a real `hls.js` npm dependency, imported directly by `lightbox.js`.
- All shared frontend files (`utils.js`, `urlState.js`, `prefs.js`, `mediaItems.js`, `region.js`, `prefsPopover.js`, `nav.js`, `accountsBar.js`, `gameSearch.js`, `lightbox.js`, `panel.js`) converted from plain global scripts to real ES modules with explicit `import`/`export`, and `app.js`/`library.js`/`bundles.js`/`gameColumns.js` updated to import from them instead of relying on load-order-dependent globals. `public/package.json` (`{"type":"module"}`) lets Node's test runner keep `require()`-ing these files unchanged (Node 22+'s synchronous `require(esm)` support), so no test files needed changes. Each page's HTML now loads a single `type="module"` entry script instead of a dozen separate `<script>` tags in a specific order. This and the Vite adoption above were done as separate steps specifically so the app kept working (verified live) at every point along the way, rather than landing in a broken intermediate state.
- Bumped `@vates/data-table-core`/`@vates/data-table-vanilla` to 0.12.0, adopting its new `initialViewState` construction option (`vatesfr/data-table#20`, `vatesfr/data-table#21`) in place of the old `defaultVisibleColumns`/`defaultPageSize` options (removed upstream). `DEFAULT_SORT` is now part of `initialViewState` on all three table constructions (Library Explorer's Library/Wishlist tabs, Bundles), which — since `resetView`/`setViewState({})` now resolve back to `initialViewState` too — removed the manual "reapply DEFAULT_SORT after reset" workaround both pages needed before.
  - The upgrade needed two follow-up fixes: `library.html`/`bundles.html`'s import map needed a new `@vates/data-table-core/internal` entry (the vanilla adapter's ESM build now imports from it at runtime); and `library.js`/`bundles.js`'s direct `processData`/`searchData` imports (used by `getGameList`'s own prev/next-nav ordering) had to move to that same `/internal` sub-path, since 0.12 split them off `@vates/data-table-core`'s public surface. That sub-path is explicitly documented upstream as unsupported/no-semver-guarantee — there's currently no sanctioned adapter-level alternative for what `getGameList` needs (the table's live filtered/sorted/searched row order, independent of its display-only grouping/pagination). Flagged as `vatesfr/data-table#22`.
- Bumped `@vates/data-table-core`/`@vates/data-table-vanilla` further to 0.13.0. `@vates/data-table-vanilla`'s new `table.getProcessedData()` (closing `vatesfr/data-table#22`) replaces the manual `processData`/`searchData` calls both pages' `getGameList` used to make — neither file imports from `@vates/data-table-core/internal` anymore (the import map still carries that sub-path entry regardless, since the vanilla adapter's own bundle imports from it internally either way). Also adopted 0.13's new column-category feature (`ColumnDefBase.category`): most of `gameColumns.js`'s `CORE_COLUMNS`/`PRICE_COLUMNS`, plus `library.js`'s Played/Last Played/Added and `bundles.js`'s Tier Price/Add-on, now carry a `category` (`Scores & Reviews`, `How Long To Beat`, `Play Time & Dates`, `Classification`, `Compatibility`, `Extras`, `Pricing`) so the Columns/Sort/Group dropdowns' 25+-column lists collapse into per-category submenus instead of one long flat list. `@vates/data-table-core` stays an explicit direct `package.json` dependency (not folded away as a transitive one) — `gameColumns.js` still imports its public `compareMissingLast`/`bucketNumericRange`/etc. directly, a genuine ongoing use, not a leftover from the internal-reach workaround above.

### Fixed

- Library Explorer and Bundles: the `@vates/data-table-vanilla` dark-theme color overrides are now set on `html:root` instead of `#table-container`. The 0.13 column-category submenus (`.dt-dd-submenu`) render via a `Portal` mounted onto `document.body`, not nested inside `#table-container`, so they weren't inheriting our container-scoped overrides and fell back to the library's light-theme defaults. `html:root` still out-specificities the library's own plain `:root` rule (needed regardless of import order), while also being an actual ancestor of the body-portaled submenu. Filed upstream as `vatesfr/data-table#23`; this is a workaround to drop once that's fixed properly.
- Lightbox now blocks every page-level keyboard shortcut while it's open, rather than each one individually opting out via its own `!isLightboxOpen()` check — a check that ArrowUp/Down (the panel's prev/next-game nav) had never had, so switching games via ↑/↓ used to still work while the lightbox stayed open on the new game's first shot, with no visible sign the game had actually changed. ArrowUp/Down is the one exception, reimplemented as one of the lightbox's *own* shortcuts (`initLightbox`'s new `onGameNav` hook) rather than being silenced along with everything else — it still pages the panel's game list, jumping straight into the new game's lightbox at shot 0, with the switch now visible via the new caption above. `R` (random pick) is deliberately left blocked for now rather than getting the same treatment.
- Lightbox: stepping to the next/previous screenshot no longer leaves the *previous* image on screen for the duration of the load — `renderLightbox` only blanked/loading-masked the image on the initial (non-animated) open, not on `stepLightbox`/`gotoLightbox` navigation, and a browser doesn't clear an `<img>`'s painted pixels just because `src` was reassigned. Now shows the shot's own thumbnail (already loaded/cached from the hero carousel) as a degraded placeholder while the full-size image loads in the background via a throwaway `Image()`, falling back to blank for the banner shot (which has no separate thumbnail). `retryCurrentShot` updated to match.
- Library Explorer and Bundles: the default sort no longer disappears on a first-ever visit (no stored table-view pref yet). `restoreTableView` was unconditionally calling `setViewState({})` when nothing was stored, which cleared the construction-time default sort right back out. Regressed in 0.4.0's move of table-view persistence to `prefs.js`; fixed for good by the `initialViewState` migration above, which is what `setViewState({})` now resolves an omitted sort back to instead of blanking it.
- All three tables (Comparison, Library Explorer, Bundles): a mismatched-color seam at the start/end of every row, from the horizontal-scroll-affordance background (`.table-wrap`/`.dt-table-wrap`) painting its opaque edge "cover" as `var(--bg1)`, on the assumption it always sits on a `.card`. True for Bundles (whose whole detail block, table included, used to be one `.card`) but not for the Comparison page or Library Explorer, both of which sit directly on `<body>` (`--bg0`) — the mismatch showed as a visibly different-colored strip at both edges of every row there. Fixed by pulling Bundles' table/status-row/"Not on Steam" section out of its `.card` (kept only around the bundle-detail header, matching Library Explorer's own card/bare-table split) so all three sit on `--bg0` the same way, then changing the cover color to match.

## [0.4.0] - 2026-08-28

### Added

- New shared `public/prefs.js`: a generic, per-key user-preference store (`getPref`/`setPref`) over a single localStorage blob (`steam-common-games:prefs`), laying the groundwork for a future Steam-auth-backed server sync (per-key, so two preferences changing around the same time can't race each other). `region.js`'s region preference now stores through it, as do the Library Explorer's and Bundles' persisted table views (see below).
- A ⚙ "Preferences" popover in the shared nav bar — one place to manage a preference independent of whatever page happens to be open. Only region lives here today; more are expected to land in the same popover later. Removed Bundles' and the Library Explorer's Wishlist tab's own inline region pickers now that this exists — both show a read-only readout of the resolved region instead, reacting to a change made here via a new shared `region.js` event (`REGION_CHANGED_EVENT`). The popover is centered under the whole nav bar rather than anchored to its own ⚙ button — confirmed live (Galaxy S10/Firefox) that anchoring to the button itself put the popover off-screen on the left, since the nav bar's items re-center as a group when they wrap on narrow widths and the button isn't reliably in the same spot. Its contents (the region `<select>` and its wiring) live in a new `public/prefsPopover.js`, not `public/nav.js` itself — `nav.js` owns only the popover's `<details>` shell and open/close mechanics, so it stays about page navigation regardless of how many preferences the panel grows to hold; `region.js`'s old `initRegionSelect` moved there too, since it was the only remaining place that populated a `<select>` at all.
- Library Explorer and Bundles: a "🔗 Share view" button next to "Reset view" copies a link with the current table view (sort/filters/columns/grouping) snapshotted into it (`?lv=`/`?wv=`/`?bv=`), for deliberately sharing an exact view. This replaces automatically writing the view to the URL on every interaction (`@vates/data-table-vanilla`'s own `syncViewToUrl`, previously used by the Library Explorer only) — the table view still auto-persists locally as it changes (now via `prefs.js` on both pages, including Bundles' `bundles:table-view`, which previously used its own separate localStorage key), but the page's own address bar is never written to — only the copied link carries the snapshot, so it can't go stale the way writing it live would once the table changes again.
- Side panel's Price card now shows a price for every game, not just rows a host page already batch-prices (the Library Explorer's Wishlist tab, Bundles) — the comparison page, standalone "look up any game" lookups, and the Library Explorer's own Library tab now fetch it lazily per-game (`panel.js`'s `loadPrice`), the same on-demand shape news/achievements already use. Requires `ITAD_API_KEY`; shows nothing when it isn't configured, same as before. Comparison page gained `region.js` (auto-detected/shared region preference) as a result, having previously had no pricing at all.
- Comparison page: the filter panel (Tag/Genre/Category/Developer/Publisher, each its own checkbox list) now collapses behind a "Filters ▾" toggle on narrow (≤768px) viewports, defaulting to collapsed — previously it always rendered fully expanded, pushing the results table roughly two screens down on a phone before any game was visible. Active-filter chips stay visible either way; unaffected on wider viewports.
- Game tables (Comparison, Library Explorer, Bundles) now show a subtle edge-fade shadow on `.table-wrap`/`.dt-table-wrap` when a horizontally-scrollable table has more columns off to one side — previously there was no visual cue at all that columns beyond the first were reachable by swiping, so most of a table's columns went undiscovered on mobile. Pure CSS (no JS), automatically hidden once scrolled to that edge.
- Screenshot/video lightbox (`public/lightbox.js`, shared by all three pages):
  - A failed image or video load now shows a "Couldn't load this ..." message with a Retry button instead of silently leaving a broken image or a stuck loading spinner. Retry re-attempts the image (cache-busted) or replays the HLS stream.
  - `.lb-counter` is now `aria-live="polite"` so screen readers announce navigation, and the image/video now carries a descriptive `alt`/`aria-label` (game name + "Screenshot/Video N of M") instead of a generic hardcoded `"Screenshot"`.
  - `Home`/`End` jump to the first/last item, alongside the existing arrow-key stepping.
  - Touch: double-tap now zooms 2× toward the tap point (or resets zoom if already zoomed in), matching the existing desktop double-click behavior — previously only pinch-zoom was available on touch.
  - Prev/next preloading now also warms a video's poster image and primes its HLS manifest in the HTTP cache, not just adjacent images, so stepping onto a video feels less cold.
  - Idle-hide chrome (toolbar, prev/next, cursor) now also applies while looking at a still image — previously it only ever armed for a playing video (`showLbVc`/`schedHideLbVc`, renamed to the media-agnostic `showLbChrome`/`schedHideLbChrome`); a paused video remains the one exception that always stays fully visible.
  - Touch: double-tapping the left/right third of a video now seeks ∓10s (with a brief "⏪10s"/"10s⏩" flash), mirroring the common mobile-player convention — previously the on-screen scrub bar was the only way to seek on touch. The middle third and single taps are unaffected (still toggle play/pause).
- `GET /api/metrics` now reports several additional monitoring signals alongside the existing outbound request/status counts: per-label `avgLatencyMs`/`maxLatencyMs`, a `rateLimiters` field tracking how often our own `express-rate-limit` instances actually rejected a client request, `dedupHits` (how often `lib/dedup.js` coalesced a concurrent call — both now share one `createNamedCounter()` helper in `lib/metrics.js`), `cacheHits` (per-cache-group hit/miss/forced counts, tracked in `lib/metrics.js` rather than `lib/cache.js` itself), `cacheEntries` (per-cache-group row counts read straight from `db.sqlite`), `semaphores` (live + lifetime-high-water-mark stats for `lib/steam.js`'s `storeLimit`/`tagLimit`/`protonLimit`, now including a `rejected` count for calls turned away by the queue-depth safety valve), and `tripCount`/`consecutive403s` on the existing store circuit breaker. `sinceRestart`/`lastHour` are the response's only time buckets, always at the root — `groups`, `rateLimiters`, `dedupHits`, and `cacheHits` all nest under one or the other rather than each windowed signal choosing its own place for the bucket. See CLAUDE.md's Monitoring section for the full shape and how to read each field.
- Two new proactive server-log warnings, logged right when they happen rather than needing someone to go read `GET /api/metrics`: `[circuit-breaker] steam-store tripped ...` (once, at the moment `lib/steam.js`'s circuit breaker actually trips) and `[semaphore:<name>] queue saturated ...` (every time a concurrency semaphore's `maxQueue` safety valve rejects a call). See CLAUDE.md's new "Server logs" section.
- Bundles page and the Library Explorer's Wishlist tab: every price column (Tier Price on Bundles, Steam Full Price, Best Deal, All-Time/1yr/3mo Low) and the Discount column are now groupable, not just filterable — price columns bucket against Steam's own common price tiers (Free / $0–5 / $5–15 / $15–30 / $30–50 / $50–75 / $75–100 / $100+, no currency symbol since the selected region isn't always USD), Discount buckets in plain 25-point steps (0–25%/25–50%/50–75%/75–100%). A bundle's "Varies" tier price (Build Your Own pick-and-mix) groups under its own "Varies" label rather than the generic "—" other price columns use for missing data.
- Same two tables: a new hidden-by-default "Price Status" column categorizes a row's current deal as All-Time Low / 1yr Low / 3mo Low / On Sale (discounted but not a historical low) / Not Discounted / — (no price data) — computed live from the same fields the Best Deal cell's 🔥/★ badge already uses (now extended to also recognize a 3-month low), so it's filterable/groupable directly instead of only eyeballed off that badge. Reuses that same badge's color/icon treatment for the record tiers (bright teal + bold + 🔥 for All-Time Low, green + ★ for 1yr Low, amber + ☆ for 3mo Low; On Sale/Not Discounted stay plain) so a genuinely record-breaking deal visually stands out from a merely-discounted one, sorted worst-to-best by a single canonical tier list shared between the column's sort order and its render styling.
- Bundles page: the game detail side panel now has the same prev/next/random nav bar (↑/↓/R), ArrowLeft/Right hero screenshot/video stepping, and `?game=<appid>`/`&shot=<idx>` deep linking `app.js`/`library.js` already had — opening a game from the table, a DLC/base-game link, or a `?game=` URL all go through one `openGame`, so the nav bar and URL stay in sync regardless of how a game was opened. A `?game=` alongside `?bundle=` opens as a real, fully-navigable row once that bundle's games have resolved (not a lesser standalone view) if the game is actually part of it, and falls back to a standalone lookup otherwise. Opening a different bundle closes any open panel and clears `?game=`/`&shot=` (a region change reopening the *same* bundle is the one exception — the open game now survives that re-price instead of getting closed).
- Game detail side panel (`public/panel.js`, shared by the Library Explorer, Bundles, and standalone game lookups): a new, deliberately light Price card — a single always-open card, not a collapsible section — showing the current Best Deal (same 🔥 all-time-low/★ 1-year-low color+badge+tooltip the table cell already uses), its Discount when there is one, and the shop name, plus an "IsThereAnyDeal ↗" link in the card title for the fuller picture (historical lows, every other shop) this card leaves out. When there's a deal to buy, the whole line (not just the shop name) is the outbound buy link — ITAD's own per-deal affiliate/redirect `url`, a newly-surfaced field on `extractPriceInfo`/`POST /api/prices`, passed through unmodified same as every other ITAD-sourced link in this app. Entirely passive: no new fetch happens in `panel.js` itself, it just renders `bestDealPrice`/`bestDealCut`/`bestDealShop`/`bestDealUrl`/`priceCurrency` whenever `library.js`'s Wishlist tab or `bundles.js` has already set them on the row object — so the card is simply absent on the comparison page and on standalone "look up any game" lookups, which never carry ITAD pricing. `loadWishlistPrices` (library.js) and `loadPrices` (bundles.js) now also re-render an already-open panel right after they mutate a row's price fields (the same `isPanelOpen() && getPanelGame() === row` idiom the ratings/HLTB stream and achievements loader already use), so a panel opened before its price batch resolves still picks the numbers up once they land instead of being stuck showing nothing.
- New **Bundles** page (`public/bundles.html`/`bundles.js`), linked from the header/footer nav on every page — browses current Steam game bundles via the [IsThereAnyDeal API](https://docs.isthereanydeal.com/) (`lib/itad.js`), optional and gated behind a new `ITAD_API_KEY` setting (the page shows a clear "not configured" message, and `GET /api/bundles`/`POST /api/bundles/resolve`/`POST /api/bundles/prices` return 503, when it's unset). A region picker (`?country=` URL param, curated list of major Steam pricing regions, defaulting to the browser's detected locale) drives which currency/prices are shown; the bundle list itself is height-capped and scrollable, and auto-collapses (with a "Show list"/"Hide list" toggle to bring it back) once a bundle is opened, so it doesn't push the actual content — that bundle's game table — down the page. Opening a bundle resolves its games to Steam appids (`POST /api/bundles/resolve`, via ITAD's shop-id lookup) and streams in ratings/HLTB/tags/store metadata through the same `POST /api/game-details/stream` pipeline the Library Explorer uses, rendered in an equivalent `@vates/data-table-vanilla` table (same column set as the Library Explorer's Wishlist columns, plus four bundle/pricing-specific ones: Tier Price, Add-on, and — from a new `POST /api/bundles/prices` route wrapping ITAD's `/games/prices/v3` — Steam Price (the non-discounted regular price) and All-Time Low, both visible by default, plus 1yr Low/3mo Low hidden by default) with the same shared side panel for game details. Games that don't resolve to a Steam listing (other-shop-only, DRM-free, or not yet mapped by ITAD) are shown separately in a plain "Not on Steam" list rather than in the table. An opened bundle also shows a "View on IsThereAnyDeal ↗" link and a "Get this bundle ↗" link (the bundle's real, unmodified shop/affiliate URL). A new `cache_bundles` SQLite table (`BUNDLES_CACHE_TTL_MINUTES`, default 2 hours) caches bundle listings, Steam-appid resolutions, and per-region price lookups. Opening a bundle writes `?bundle=<id>` to the URL, and loading the page with that param deep-links straight to it — since ITAD has no single-bundle-fetch endpoint, `GET /api/bundles/:id` (`findBundleById` in `lib/itad.js`) pages through the same bundle list server-side (active bundles first, then expired, up to a bounded number of pages) until the id turns up, 404ing rather than searching indefinitely if it never does. A bundle tier with no fixed price at all (Fanatical's "Build Your Own ⟨N⟩ Bundle" pick-and-mix format — a pool of games, price scales with how many you pick, never a property of the bundle itself) now correctly shows "Varies"/"Price varies" instead of "Free" in both the bundle list and the Tier Price column. Prices follow whatever currency each store actually charges for that bundle — most stores do switch currency with the region picker, but a few (e.g. some Fanatical/Digiphile-published bundles) only ever price in USD regardless of region; a note next to the region picker explains this. The bundle-routes rate limiter (`BUNDLES_RATE_LIMIT_MAX`) is now 4 separate per-route limiters, each skipping requests it can already answer entirely from cache — same "cache hits don't count" treatment `DETAILS_RATE_LIMIT_MAX`/`GAME_SEARCH_RATE_LIMIT_MAX` already had — instead of one shared limiter that counted every request regardless of whether it made an upstream call; simply reloading the page a few times used to burn the whole per-minute budget on requests that never touched ITAD, then 429 the next genuinely new bundle opened. A failed Steam-price lookup (e.g. hitting that same budget) no longer leaves the Steam Price/All-Time Low/1yr Low/3mo Low columns stuck on their loading placeholder forever — they now show "—" and a visible (not just console-logged) explanation, same as any other "couldn't load this" state elsewhere in the app. Fixed a real rendering bug behind reports of those same columns simply never appearing even on success (confirmed live: the row data was correct, but its table cells stayed on their loading placeholder) — `@vates/data-table-vanilla`'s `setData` only re-renders a row's cells when the object at its rowKey is a genuinely new reference; `loadPrices` mutates the existing row object in place (needed elsewhere — the panel, achievements cache — which rely on that object staying the same reference), so a row already made visible by the ratings/HLTB stream before the price lookup resolves (the common case once everything is cache-warm and both fire near-instantly) never picked up the update. Every `table.setData()` call in `bundles.js` now goes through a new `visibleRowsForTable()` helper backed by a per-appid `tableRowCache`: a row keeps the same cached copy across renders until something actually mutates it (`markRowChanged`, called at every mutation site — the ratings/HLTB stream's own first reveal included, not just `loadPrices`/refresh), so the table only re-renders the row that changed rather than every visible row on every event, and `rows`/`rowMap` keep the stable object identity the panel and achievements cache depend on. (An earlier version of this fix blanket-copied every visible row on every render, which technically fixed the staleness but forced a full-table re-render on every streamed event and briefly broke the panel's own object identity — replaced with this targeted version in the same session.) Added a "Best Deal" column (visible by default) and a hidden-by-default "Best Deal Shop" alongside it — the cheapest *current* price across every shop ITAD tracks for that game, Steam included (as opposed to Steam Price, Steam's own non-discounted list price, or the historical lows, which reflect what something has sold for at some point, not necessarily today) — from the same `/games/prices/v3` response, extracting the lowest-priced entry in `deals[]` rather than only Steam's own.
- Bundles: the game table's view (visible columns, sort, grouping, filters) now persists across bundles and page reloads via `persistViewToLocalStorage` (`@vates/data-table-vanilla`), plus a "Reset view" button to clear it back to the defaults — same pattern the Library Explorer already uses, but via `localStorage` (`bundles:table-view`) instead of a URL param, since there's no per-bundle URL state to hang it on (`?bundle=` deep-links to *which* bundle, not the table's own display preferences) and the preference is meant to carry over from whichever bundle was open last to the next one anyway.
- Bundles: "‹"/"›" buttons next to the bundle title step to the previous/next bundle in the currently-loaded list (respecting its current sort/filter) — disabled at either end, and both disabled entirely for a bundle that isn't part of the loaded list at all (e.g. a `?bundle=` deep link to one that hasn't been paged into view). The "Steam Price" column is renamed "Steam Full Price" to make clear it's the non-discounted list price, not whatever Steam happens to be charging today. The "Best Deal" column no longer shows the shop name inline (still available as its own hidden-by-default "Best Deal Shop" column, and now in a short hover tooltip on the price itself — just the shop name, plus " — all-time low"/" — 1-year low" when it's also one of those, deliberately without restating "cheapest current price" since that's already what the column means) — instead the price is colored/badged when it's at or below a historical low (bright teal + 🔥 for an all-time low, green + ★ for a 1-year low), so "is this actually a good deal" reads at a glance without needing to cross-reference the All-Time Low/1yr Low columns; All-Time Low is hidden by default now that Best Deal's own indicator covers the common case (the exact number is still there for anyone who wants it, just not shown by default alongside 5 other price-ish columns). The default sort is now Tier Price ascending *then* Weighted Rating descending (was Tier Price alone), so same-priced games land best-rated-first instead of in an arbitrary relative order.
- Library Explorer: the Wishlist tab now shows the same IsThereAnyDeal-backed price columns as the Bundles page — Best Deal and Discount (visible by default), plus Steam Full Price/All-Time Low/1yr Low/3mo Low/Best Deal Shop (hidden by default) — with a new region picker next to the tabs, shown only on the Wishlist tab and only when `ITAD_API_KEY` is configured. Changing region re-prices the currently-loaded wishlist in place rather than reloading it. Backed by a new shared `POST /api/prices` route (replacing the Bundles-only `POST /api/bundles/prices`) that accepts either `gids` (Bundles, unchanged) or `appids` (Wishlist) — a wishlist row only has a Steam appid, so the route resolves those to ITAD game ids server-side first (`resolveItadIds` in `lib/itad.js`, the reverse of the existing `resolveSteamAppIds`, via ITAD's `POST /lookup/id/shop/{shopId}/v1`) before running the identical price lookup; each appid's resolution is cached individually (`itad-gid:{appid}`), same tier as the existing appid→gid mapping. `library.js` also picked up the same per-row `tableRowCache`/`markRowChanged` render-staleness fix `bundles.js` already had (see below), needed once Wishlist price loading gave it a second source that mutates an already-visible row (alongside the pre-existing "↻ Refresh" button, which had the same latent risk but never actually hit it before).
- Bundles/Wishlist pricing: a new "Discount" column (`bestDealCut`, visible by default) shows how much cheaper the current best deal is than Steam Full Price (e.g. "-70%"), computed client-side (`discountPct` in `bundles.js`/`library.js`) rather than taken from ITAD's own per-deal `cut` field — `cut` is that shop's own discount off *its own* regular price, which shops set independently and isn't consistent from row to row, whereas Steam Full Price is one stable reference every row can be compared against consistently. A deal that isn't actually any cheaper than Steam (`0%`) renders as "—", same as missing data, rather than a noisy "0%". Steam Full Price is now hidden by default on both pages — Best Deal + Discount together already answer "is this worth buying, and by how much" without it.
- Region selection (Bundles and the Library Explorer's Wishlist tab) is now a shared `localStorage` preference (`public/region.js`, a new module shared by both pages) rather than a per-page `?country=` URL param — picking a region on either page updates both, since it's "what currency do I want to see prices in" rather than page-specific display state, and a shared link no longer silently overrides whatever region the person opening it actually wants. The picker's first entry is now an explicit "Auto-detect (⟨region⟩)" option — always showing what auto-detection currently resolves to — and is the default until a real region is picked; picking one overrides auto-detection until switched back. The Eurozone entry's label now leads with "Europe" ("Europe / Germany (EUR)", was "Germany / EU (EUR)").
- New `lib/metrics.js` counts every outbound request to a third-party service (Steam, HLTB, ITAD, ProtonDB), nested by trust-tier/routing group then by the specific function making the call (e.g. `itad.resolveItadIds` vs. `itad.getPrices`) — in-memory only, resets on restart. Every fetch call site in `lib/steam.js`/`lib/hltb.js`/`lib/itad.js` now goes through a new `trackedFetch(group, label, url, opts)` wrapper instead of calling `fetch` directly. Each label tracks the full breakdown — `requests`, `statusCounts` (raw HTTP status code counts, not collapsed into one "errors" number — several endpoints treat specific non-2xx codes as expected outcomes, not failures) and `networkErrors` (fetch() itself throwing) — at two time buckets, `sinceRestart` (lifetime) and `lastHour` (a rolling window), which sit at the *root* of the response (`{ since, sinceRestart: { groups }, lastHour: { groups }, circuitBreakers }`) rather than nested per-label, so one granularity can be reviewed in isolation. Exposed via a new `GET /api/metrics` route, which also surfaces `lib/steam.js`'s existing steam-store 403-storm circuit breaker state (`circuitBreakers['steam-store'].blockedUntil`) directly, via a new `getStoreCircuitBreaker` export.
- Library Explorer: an "Achievements" column (hidden by default) showing Steam's own achievement count for each game, from the `achievementCount` now extracted from store metadata — no extra request, since it rides on the same appdetails fetch already made for every row. This is the game's total achievement count, not any account's unlock progress (that's still the side panel's own Achievements section).
- Library Explorer: three more hidden-by-default columns, all likewise free byproducts of the same appdetails fetch — "DLC Count" (length of the already-fetched DLC appid list), "Platforms" (native Windows/Mac/Linux support, newly extracted as `platforms` in `lib/steam.js` — distinct from the existing ProtonDB column, which is Linux/Deck compatibility *through Proton*, not native support), and "Languages" (newly extracted as `languages`, parsed from Steam's HTML-ish `supported_languages` string).
- Library Explorer: a "Demo" column (hidden by default, leading the Extras section) showing whether the game has a free demo, from the same `event.demo` field the side panel's own "🎮 Try the Free Demo" banner already uses — no extra request. Rendered as a colored badge (`renderDemoBadge`) in the app's accent blue, the same color the panel's own demo banner uses, rather than an emoji glyph in plain text.
- Renamed the `.proton-badge` CSS class to `.status-badge` (`public/style.css`, `public/library.js`) now that it backs two columns' pills (ProtonDB, Demo) instead of one — the old name implied it was ProtonDB-specific (and, per its own stale comment, shared with the side panel, which it never actually was — the panel's own ProtonDB indicator is a separate `glanceChip`).
- Library Explorer: a "Production Tier (est.)" column (hidden by default) estimating AAA/AA/Indie from launch price, review volume, and Metacritic presence — a best-effort heuristic (see `computeProductionTier` in `public/utils.js`), not authoritative data Steam itself provides, and null (rendered "—") for DLC/expansion appids or a priced game with no price data at all. Backed by two new fields extracted from the same appdetails response already fetched for every game — `isFree` and `priceInitial` (the undiscounted launch price, in cents; `getAppDetails`'s upstream fetch now pins `cc=us` so this is always USD) — in `lib/steam.js`'s `extractAppDetails`.
- Library Explorer: a "Type" column (hidden by default) showing Steam's own content-type for the appid — Game, DLC, Soundtrack, Video, Series, Episode, Mod, Hardware, or Advertising — from a newly-extracted `type` field (`lib/steam.js`'s `extractAppDetails`, free byproduct of the same appdetails fetch). Lets a library/wishlist be filtered down to base games only, or audited for stray non-game entries (soundtracks and videos can be genuinely owned/wishlisted appids, not just DLC). `computeProductionTier` (`public/utils.js`) now also treats these non-game types the same way it already treated DLC — returning null rather than scoring a soundtrack/video's price as if it were a game — since the previous `isDlc` check (`meta.fullgame != null`) only caught DLC's back-reference to a base game, not these other types.
- Side panel: a "🎮 Try the Free Demo" banner for games that have one — a standalone, accent-colored call-to-action right under the sticky header (not tucked into the "⋯ More" icon menu alongside Website/Workshop, which are supplementary info rather than an action worth surfacing without an extra click) — scrolls away with the rest of the body instead of permanently occupying pinned header space for the games that have it. `getGameDemo` (`lib/steam.js`) reads `related_items.demo_appid` off the same `IStoreBrowseService` store browse item already fetched for tags, so this costs no extra upstream request. That item is now cached whole (`browse:{appid}`, replacing the tags-only `tagids:{appid}` cache key) rather than just the extracted tagids — the usual "cache raw, extract at read time" approach used elsewhere in `lib/steam.js` — precisely so a later addition like this one never needs a new request, just a new extractor over the already-cached response.
- Side panel: an All/Unlocked/Locked filter for the expanded Achievements list, so a game with a large achievement count (100+) doesn't force scrolling through everything just to find what's left to unlock. Only shown once real progress is loaded (filtering by achieved status is meaningless with nobody loaded, where nothing is ever marked achieved). The list is now also sorted once per fetch and cached rather than re-sorted on every render — any unrelated toggle elsewhere in the panel re-renders the whole body, which used to re-sort the full achievement list every time for no reason.
- Side panel: a 🔗 icon-link next to Store/IsThereAnyDeal copies a link straight back to this game (`?game=<appid>` on the current page, briefly flashing ✓ on success) — deliberately just the appid, not the full current URL with whatever `u=`/filters/sort/tab/view happen to be set from the search that led here, since sharing "check out this game" almost never means "reproduce my exact search too".
- Side panel (`public/panel.js`, shared by both pages): a new "DLC" card lists a base game's downloadable content — collapsed by default, same as HLTB/News/Achievements. The collapsed header's count comes straight from the base game's own store metadata (`meta.dlc`, a bare appid list Steam's `appdetails` response already includes for free); the full list — each entry's name and capsule image — is only resolved once the card is actually expanded. Each DLC appid is resolved through the exact same `GET /api/game-details/:appid` (and its existing `detailsLimit` rate limiter/cache/circuit-breaker) every other single-game lookup already uses, rather than a bespoke batch endpoint — a DLC appid isn't fundamentally different from any other appid the app looks up, so it isn't given its own rate-limit policy or an arbitrary cap on how many resolve at once; a franchise with a very large DLC catalog is subject to the same per-minute budget as any other burst of game lookups, same as rapidly opening that many panels by hand would be. A DLC entry that fails to resolve (delisted, or rate-limited under a large burst) is silently dropped from the list rather than shown as an error — the rest is still worth showing. Each entry is a real link (`<a href>`, so ctrl/cmd/shift-click and middle-click open it in a new tab the normal way) that, on a plain click, opens that DLC in the same panel instead — and the panel now remembers where you came from: a "← *Game Name*" button appears above the title while a DLC-navigation trail is in progress, popping back through however many hops were taken (DLC → DLC → …), reusing the exact same "open this appid" mechanism as the "look up any game" search box. An ordinary panel open (a table/row click, a search-box pick, prev/next/random) starts a fresh trail rather than inheriting whatever was there before. The card's expanded/collapsed state (like HLTB/News/Achievements) is remembered per appid for the rest of the session — navigating to a game whose DLC card was left expanded from an earlier visit (e.g. reached again via a DLC link or the "← Back" button) now correctly kicks off the fetch itself rather than rendering the card already open with nothing loaded and no click left to trigger it.
- Side panel: a game that is itself DLC now shows a "DLC for *Base Game*" link right under the title, the reverse of the DLC card above — same real-link/intercepted-click in-panel navigation (renamed from `navigateToDlc` to the more general `navigateToGame`, since it now backs both directions of this relationship), same "← Back" support. Backed by a new `fullgame` field on `extractAppDetails` (`lib/steam.js`) — Steam's `appdetails` response for a DLC app already includes `{appid, name}` pointing at its base game, for free on the same response, so no new upstream call. The base game's name is exposed alongside its appid purely to avoid a title flash while the link is up but not yet clicked — same trade-off already made for DLC entries and the "look up any game" search box; the real name is still always re-resolved from the base game's own metadata once you actually open it.
- Side panel: two new icon-links alongside the existing Steam Store/IsThereAnyDeal/News icons — a 🛠️ Steam Workshop link (shown whenever `meta.categories` includes "Steam Workshop", already-fetched data reused rather than a new call) and a 🌐 Official Website link (a new `website` field on `extractAppDetails`, present for plenty of games and absent for others, same free appdetails response).
- Side panel: a 🔎 "More Like This (Steam)" link in the "⋯ More" menu, pointing at Steam's own first-party `recommended/morelike/app/<appid>` page — the exact recommendation surface the store page itself embeds. Investigated computing/sourcing a "similar games" list ourselves first (Steam's `IStoreBrowseService` has no such RPC at the protocol level; steampeek.hu and gmndx.com both turned out to be small third-party sites with no documented API, and gmndx.com's `robots.txt` explicitly opts out of AI-agent crawling) — a plain link to Valve's own page sidesteps all of that with no new upstream call, no scraping, and no trust-tier risk, at the cost of leaving the panel once clicked. Unconditional (needs only the appid, unlike the Workshop/Website links above which wait on store metadata).
- Library Explorer: the game detail side panel now has an "Achievements" section showing each game's full achievement list (name, description, icon) with unlocked/locked state and an overall N / total (%) count, for whichever account(s) are currently loaded. A merged Steam Family slot unions progress the same way owned games are unioned elsewhere — an achievement counts as unlocked for the group if any member has it. The section is collapsed by default and remembers whether you expanded it per game for the rest of the session, so a 40-100 item list doesn't dominate the panel for every game that has one. The summary and the list live in one bordered card (matching the glance grid's bold, color-coded-percentage chip look via the same `scoreColor` used for ratings) instead of a floating header above a separate stack of individually-boxed items — the list, once expanded, sits below a divider inside that same card, and its rows are separated by thin dividers rather than each being its own box. A ↗ icon next to the header links out to that account's own Steam achievements page. An unlocked achievement's card shows its unlock date on hover. A hidden achievement not yet unlocked shows "Hidden achievement" (name/description withheld, same as Steam's own profile) until clicked, which reveals it in place. Each achievement also shows its community-wide unlock percentage (e.g. "4.2%") — visible even while an achievement is still spoiler-hidden, since knowing how rare it is doesn't give away what it actually is. The list itself (names/descriptions/icons/rarity) is store metadata, not tied to any account, so it's shown even with no player loaded (e.g. a standalone "look up any game" lookup) — `steamids` on the endpoint is optional; the header shows "— · N total" instead of a percentage, and the expanded list reads "Load a player above to see who's unlocked what" instead of implying 0% progress. A new `playerCount` field on the response distinguishes "no player was asked about" from a player whose progress is genuinely unavailable (`private:true`, unchanged). New endpoint `GET /api/achievements/:appid?steamids=...` (steamids optional), backed by three new `lib/steam.js` functions: `getGameSchema` (achievement names/icons — store metadata, cached like rating/HLTB/tags), `getPlayerAchievements` (one account's unlock state per game — player progress, cached like owned games), and `getGlobalAchievementPercentages` (community-wide rarity per achievement, cached like the schema; a transient failure here degrades to "no rarity data" rather than breaking the rest of the panel). Comparison page unaffected — its groups have no single well-defined player to fetch progress for.
- Side panel redesign (`public/panel.js`, shared by both pages): a "glance strip" — a fixed 2×2 grid of SteamDB, Metacritic, HLTB, and Linux/Deck (ProtonDB) chips right under the title, each chip built from one shared template (a value, then a one-line caption) and each chip *is* the link to its source. This replaces the old Score/Linux-Steam-Deck/HLTB sections plus a separate "Links" list that duplicated the same four sources a second time. Title/release date/glance strip now stay visible (`position: sticky`) while the rest of the panel scrolls underneath. Tags/genres/categories/developer-publisher are merged into one pill cloud (`tagCloud()`) distinguished by a small colored dot + legend instead of four near-identical "uppercase title + pill row" sections. The nav bar (prev/next/random) is now pinned to the bottom of the panel on both desktop and mobile — previously it sat between the hero and the body, so reaching it on desktop meant scrolling back up past the hero every time.
- Side panel: the How Long To Beat breakdown and News sections now use the same collapsed-by-default "chip + expand-in-place" card the Achievements section already had (`collapsibleCard()` in `public/panel.js`, a small helper shared by all three) instead of always rendering their full content — a game with tags, HLTB, news, and achievements data no longer forces a long scroll just to see what's below the glance strip. Neither the HLTB nor the News chip has a headline number — HLTB has no single figure that isn't either an arbitrary pick among Main/Extra/Completionist or a plain repeat of the glance strip's own "All PlayStyles" chip just above, so it's a plain text teaser naming which categories the breakdown has instead (auto-expanding when `all` itself is missing, since then the breakdown is the only HLTB data the panel has at all); News dropped a "3 recent posts" count once it became clear the number never varies (the panel always shows every fetched headline, so a count added nothing you couldn't already see one click away). News' caption spells out "more on Steam" explicitly instead of leaving that to the header's ↗ icon's title tooltip alone, and now shows every headline the server fetched rather than truncating client-side further — a limit that existed only because the section used to always render inline and a longer list ate into the panel's visible height, which collapsing it by default made moot. `extractNews`' own cap (`lib/steam.js`) is raised from 5 to 20 to match — the upstream request already fetches `count=20`, so returning all of them (instead of throwing most away after already paying for the fetch) costs nothing extra; the panel comfortably shows the full 20 in its scrollable body once expanded. A new sticky sub-nav row (Overview / Owners / HLTB / News / Achievements, whichever are present) sits under the title and jumps straight to a section — shown only once there are at least two sections worth jumping to, so a game with nothing below the glance strip doesn't get a lone useless button.
- Side panel: the Achievements card's outbound ↗ link, previously silently dead (the server response never actually included the `steamUrl` field the frontend was already reading), now links to Steam's public per-game achievements page (`steamcommunity.com/stats/<appid>/achievements/`, no login/account needed) whenever no specific account is loaded (e.g. a standalone "look up any game" lookup) — a loaded account's own achievements page (set client-side in `public/library.js`) still takes precedence when one's available.
- Comparison page: the side panel's "Owned by" list (`buildOwnersHtml`, `public/app.js`) is now ordered by last played, most recent first (ties broken alphabetically), and each owner gets a playtime meter — a single-hue bar sized relative to the top playtime among that game's owners — as a secondary cue alongside the existing hours-played number, which stays the primary, always-visible value.
- Library Explorer: a new "Last Played" column showing the most recent time anyone in the loaded slot launched each game, sourced from `rtime_last_played` on Steam's `GetOwnedGames` response (already fetched for every game, just unused until now — no new upstream call). For a merged Steam Family slot this is the max across every account in the slot, not any one account's own timestamp. Shown as a plain date (e.g. "2026-07-01"), same convention as the Released/Added columns; blank means owned but never launched by anyone in the slot, and the column is dropped entirely from the Wishlist tab (wishlist games aren't owned, so there's nothing to have played). The comparison page's side panel "Owned by" tags gained the same per-account info (`· last played 2026-07-01`) next to existing playtime.
- Library Explorer: the Released column now colors an unreleased game's date/placeholder amber (reusing `scoreColor`'s existing mid-tier amber), so scanning the wishlist for what's still coming makes released vs. unreleased visible at a glance instead of requiring a close read of each date string. Backed by a new `comingSoon` field on `extractAppDetails` (`lib/steam.js`) carrying Steam's own `coming_soon` flag through to the client, rather than re-deriving release status by parsing the (sometimes unparseable, e.g. "Coming soon") date string.
- Side panel (`public/panel.js`, shared by both pages): a new "News" section shows up to 3 recent headlines (title, date, feed label) for the open game, each linking straight to the full post, plus a "More news →" link to that game's full Steam news hub. A new 📰 icon link next to the existing Steam Store/IsThereAnyDeal icons links to the news hub too, independent of whether any headlines loaded. `GetNewsForApp`'s raw response mixes the developer/publisher's own Community Announcements posts with syndicated third-party press coverage in whatever language that outlet publishes in, with no regard for the requester's locale — for some games that press can outnumber official posts 10:1 or more, dominating the most recent items entirely with untranslated foreign-language articles, and none of it actually matches what the store page's own News tab shows. Fixed with an undocumented `feeds=steam_community_announcements` query param that filters server-side to official posts only (verified directly against the live API) — `extractNews` (`lib/steam.js`) also keeps a `feedname` filter as cheap insurance in case that param is ever dropped or inconsistent for some app. Backed by a new `getGameNews` (`lib/steam.js`, wrapping the documented, key-less `ISteamNews/GetNewsForApp` endpoint) and a new dedicated `GET /api/game-news/:appid` route — deliberately its own endpoint rather than folded into `fetchGameDetails`/the SSE stream, since news has no table column and nothing to sort/filter on, so fetching it for every game in a whole loaded library/comparison would be pure waste for the (common) case of most panels never being opened. It's fetched lazily instead, once per game, only when that game's panel actually opens (`loadNews` in `panel.js`, same on-demand shape achievements already use), and refetched by the panel's "↻ Refresh" button alongside everything else. Cached under a new `news:` prefix with its own TTL (`NEWS_CACHE_TTL_MINUTES`, default 6 hours) — shorter than the 60-day store-metadata tier, since news/patch notes change far more often than a game's genres or screenshots.
- Cross-page navigation: index.html's header now has a "Library Explorer →" link (mirroring library.html's existing "← Comparison tool" back-link, both now sharing one `.header-nav-link` style in `style.css` instead of a page-scoped one-off). Each link is contextual — index.html's points at `/library.html?u=<ids>` for the single player/Family currently loaded (falling back to the bare link for a multi-player comparison, which the Library Explorer has no notion of), and library.html's points at `/?u=<ids>` for whoever's currently loaded there.
- Both pages now keep the browser tab title in sync with what's actually open: an open game's name takes over the title entirely (e.g. "Half-Life 2 — Steam Common Games" / "Half-Life 2 — Library Explorer"); with no panel open it falls back to the loaded player/comparison context (e.g. "alice, bob — Steam Common Games" or "alice's Wishlist — Library Explorer"); with nothing loaded at all, the original static title. Makes finding a specific game/search back through browser history or tab-search actually work, instead of every tab reading the same generic title.
- Library Explorer: the side panel now has the same "Owned by" section the comparison page already had (`buildLibraryOwnersHtml` in `public/library.js`, same template as `app.js`'s `buildOwnersHtml`) — per-account playtime and last-played, most recently played first, with a playtime meter. Shown even for a single loaded account (not just merged Steam Families) for consistency between the two pages; for a Family slot it surfaces the per-member breakdown the table itself flattens into one summed playtime / max last-played column. Backed by the same per-account `playtime`/`lastPlayed` maps `/api/common-games` already returns (previously only used to compute those flattened table numbers, then discarded) — works automatically for a standalone "look up any game" lookup too, since those maps cover the loaded account's entire owned library, not just whichever games are currently visible as table rows. Empty (and correctly hidden) on the Wishlist tab, since wishlist items aren't owned by anyone.
- `public/urlState.js` gained `reorderUrlParams`, a canonical query-param order (`u`, `tab`, `sort`, `game`, `shot`, `name`, filter dims, `view`, `wview`) shared by every URL-writing function on both pages — the same logical state now always serializes to the same URL string regardless of what order its pieces happened to be set/mutated in.
- Library Explorer now supports browser back/forward (previously the URL changed on load/tab-switch/panel-open but nothing listened for `popstate`, so Back did nothing). Also collapsed what used to be 2-3 separate `pushState` calls per Load-button click / tab switch / recent-search click (clearing `game`/`shot`, then setting `u`, then `tab` — each its own history entry) into a single push per genuine new load, with everything else (panel open/close, lightbox, an account refresh, restoring from a URL/back-forward navigation) using `replaceState` instead.

### Changed

- Library Explorer's table-view URL params renamed `view`/`wview` → `lv`/`wv`, for a short, consistent naming scheme with Bundles' new `bv` (see above). Old `?view=`/`?wview=` links no longer apply.
- Region preference no longer migrates from its pre-`prefs.js` localStorage key — an existing user's region resets to Auto-detect once.
- Weighted Rating, Wilson Score, Steam %, and Metacritic Score (`public/gameColumns.js`) are now groupable with a shared fixed 5-point bucket scheme (`scoreBucket`/`formatScoreBucket`), everything below 60 collapsed into one "< 60" bucket — replacing Weighted Rating's previous `groupable: false` (grouping the raw unrounded value would've been near-useless) and the other three's previous ungrouped/exact-value grouping. Verified against a real 1,131-game library that the plain 25-point step Discount uses would put 75-86% of games in a single 75-100 bucket for these columns; 5-point steps spread that same library across 9 comparably-sized buckets instead.
- Column label naming pass for consistency in `public/gameColumns.js`: `developers`/`publishers` are now labeled "Developers"/"Publishers" (were singular "Developer"/"Publisher" despite holding array values, unlike every other multi-value column); `achievementCount` is now "Achievement Count" (was "Achievements", the only one of the three "___ Count" columns not following that pattern); `lowY1`/`lowM3` are now "1-Year Low"/"3-Month Low" (were the abbreviated "1yr Low"/"3mo Low", inconsistent with `lowAll`'s spelled-out "All-Time Low"). No functional/behavior change — labels only.
- `DEAL_RECORD_TIERS`' `statusLabel` (`public/utils.js` — the Price Status column's cell text, and one of the three places `dealRecordTier` keeps in sync) now reads "1-Year Low"/"3-Month Low" instead of the abbreviated "1yr Low"/"3mo Low", matching the `lowY1`/`lowM3` column labels above and the tooltip wording (`tooltipLabel`, unchanged) it sits right next to on the same tier object — the two had drifted to different abbreviations for the same thing. `renderBestDeal`'s tooltip and the side panel's Price card both read `tooltipLabel` off this same shared source, so they pick up consistent wording automatically.
- The Library Explorer and Bundles pages now share their entire game-table column set via a new `public/gameColumns.js` module (`CORE_COLUMNS` — identity/scores/HLTB/dates/classification/compatibility/extras, shown on all three views — and `PRICE_COLUMNS` — Wishlist and Bundles only), instead of each maintaining its own near-identical copy of ~35 column definitions and their render/format/compare/bucket helpers. Each page layers its own page-specific columns (Bundles: Tier Price/Add-on; Wishlist: Rank/Added; Library tab: Played/Last Played) onto the shared base via `insertColumnsAfter`. `formatMoney` moved to `public/utils.js` so `panel.js`'s Price card (a plain script that can't `import` from `gameColumns.js`, an ES module) can keep sharing it. No behavior change — verified live that both pages' tables, column pickers, and the side panel's Price card render identically to before across Bundles, the Library tab, and the Wishlist tab.
- The "given a price and its historical lows, which record tier does it qualify for" logic (color/icon/tooltip text for All-Time/1yr/3mo Low) is now one shared `dealRecordTier` function in `public/utils.js`, used by `renderBestDeal` (bundles.js + library.js), the side panel's Price card, and the Price Status column instead of each hand-rolling its own copy — the 3-month-low bug just above is exactly the kind of divergence that duplication caused (the tier was added to Price Status first and silently missed everywhere else). Added unit tests for it in `test/frontend-utils.test.js`.
- `CLAUDE.md`'s Development workflow section now notes not to re-run `npm test` manually right before `git commit` once step 1 has already confirmed tests pass — the pre-commit hook re-runs it and blocks on failure regardless, so a run whose only purpose is "will this commit succeed" is redundant with the hook.
- Replaced the per-page header corner links and footer link row (two separate, mutually inconsistent nav mechanisms — each page linked to a different subset of the other three, worded differently) with one shared top nav bar (`public/nav.js`, `initNav`/`updateNavLink`) on all four pages — Comparison, Library Explorer, Bundles, About — sitting above the header title as its own row. The current page renders as a plain highlighted label rather than a link to itself; the Comparison ⇄ Library Explorer links still carry the currently-loaded player's `?u=` along, now centralized in `updateNavLink` instead of duplicated per page. Footers now show only the keyboard-shortcuts hint where one applies (index/library); bundles.html/about.html had no hint to begin with, so their footers are removed outright.
- `CLAUDE.md`: added "Working style", "Knowledge sharing", "Git workflow", and "Development workflow" sections (working preferences, commit conventions, and a post-change checklist), plus a note on running the test suite (`npm test`) and the absence of a linter/formatter — integrated from a personal cross-project `CLAUDE.md` template. The template's "use a dedicated branch for complex changes" bullet was dropped rather than adopted — this repo has never merged a feature branch back into `main`, even for large multi-file changes, so it documents committing directly to `main` instead. Added a `.git/hooks/pre-commit` script (not version-controlled, so it needs recreating after a fresh clone) that runs `npm test` and blocks the commit on failure.
- Escape-key handling (close the lightbox if open, else close the panel) is now one shared `panelHandleEscape()` in `public/panel.js`, called from `app.js`/`library.js`/`bundles.js`'s own keydown handlers instead of each hand-rolling its own copy — the bundles.js Escape/lightbox fix above surfaced that exact duplication as a real bug (one of three copies had silently drifted), so it's consolidated into one place that can't drift again; each page still owns its own higher-priority checks (e.g. the keyboard-shortcuts modal) before falling through to it.
- The "SteamDB Rating" column (Library Explorer) and glance chip (side panel) are renamed to "Weighted Rating" / "Weighted" now that the underlying formula (`computeSteamdbRating`, `public/utils.js`) has diverged from SteamDB's own published one and no longer shows the same number SteamDB itself would. Internal identifiers (`computeSteamdbRating`, the `steamdbRating` row key, `?sort=steamdbRating`) are left unchanged to avoid breaking already-shared URLs.
- The side panel's Weighted Rating glance chip now links to the game's Steam reviews instead of its SteamDB page, since the number/caption is this app's own weighted rating, not SteamDB's — SteamDB is no longer a relevant destination for it.
- SteamDB Rating (`computeSteamdbRating`, `public/utils.js`) now converges to the raw Steam % faster than SteamDB's own published formula: the shrinkage-toward-50%-prior term uses a new `STEAMDB_RATING_SHRINK_STRENGTH = 2` exponent instead of SteamDB's implicit `1`, so a review sample is trusted sooner (e.g. 90% positive over 100 reviews now lands around 87-88%, vs. 80% under SteamDB's own formula). Still mathematically bounded between 50 and the raw ratio for any review count.
- Library Explorer: every bucketed group-by column ("Review Count", "Played (h)", "All/Main/+Extra/100% (h)", "Last Played", "Released") and every multi-value column ("Genres", "Categories", "Tags", "Developer", "Publisher", "Languages", "Platforms") now stays visible as its own row cell even when grouped by, via `keepVisibleWhenGrouped` (new in `@vates/data-table-core`/`@vates/data-table-vanilla` 0.11.0), instead of being auto-hidden the way a plain grouped column is. For a bucketed column the group header only shows the bucket label (e.g. "3–10h"), which used to leave the row's exact value visible nowhere; for a multi-value column a row fans out into one group per value (e.g. a "Tags" row shows up under both "Roguelike" and "Deckbuilder"), which used to hide the very column needed to see a row's *other* values while looking at one particular group.
- Library Explorer: the "Demo" column is now visible by default on the Wishlist tab (still hidden by default on the Library tab) — "can I try this before buying" is more relevant to games you don't yet own.
- Library Explorer: adding a column to Group by now also sorts by it (using the column's own `defaultSortDir`) — grouping without sorting left rows in an arbitrary order within each group. Only fires for a genuine live click on the Group menu, not when a saved/URL/back-forward view happens to be restored with a group and its own, deliberately different sort already set (`bindGroupBySort` in `public/library.js`, both tabs).
- Library Explorer: with 20+ columns now available, `COLUMNS`/`WISHLIST_COLUMNS` (`public/library.js`) are reordered into logical sections — Identity, Scores & reviews, How Long To Beat, Play time & dates, Classification, Compatibility, Completionist extras — instead of roughly the order each was added to the codebase, so the column picker and table read as a coherent list rather than a grab bag. Wishlist Rank and Added now sit inside the sections they actually belong to (right after Name, and right after Released) instead of trailing at the very end.
- Library Explorer: grouping by "Review Count", "Played (h)", "All/Main/+Extra/100% (h)", "Last Played", "Released", or "Added" now buckets rows into ranges (`groupValue`/`groupFormat`, new in `@vates/data-table-core` 0.8.0) instead of grouping by each row's exact value, which for these continuous/near-unique columns produced close to one row per group. "Review Count" and "Played (h)" use half-decade ("1-3-10") log-scale buckets — checked against a real library, plain base-10 decades put the entire 1h-10h range (roughly half of any typical player's played library) into one indistinguishable bucket; log buckets fix that while still collapsing cleanly up into the millions of reviews / thousands of hours some games/players reach. Everything under 1h collapses into a single "<1h" bucket rather than continuing the log staircase below it, since sub-hour resolution (2 minutes vs. 40) isn't a distinction anyone actually cares about. The HLTB columns use plain linear 10h buckets instead — their range is naturally bounded (mostly 0-150h), so a fixed step stays meaningful throughout, unlike the other two. The three date columns bucket by year (`bucketDatePart('year')`). A column's own missing/never-happened value (a failed rating fetch, no HLTB match, an unparseable release date, "never played") is kept in its own "—"/"Not played" group rather than being silently coerced into a real bucket, which needed an explicit guard (`withMissingGroup`/`formatMissingGroup`) since a raw `null` reaching `bucketNumericRange`/`bucketDatePart` unguarded stringifies to the literal text `"null"` instead.
- Library Explorer: "Name" is no longer groupable — a game's name is (almost always) unique per row, so grouping by it produced one row-sized group per game, same as the other near-unique columns above before their bucketing was added.
- `GET /api/achievements/:appid` now checks the game's already-cached store metadata first and skips `GetSchemaForGame`/`GetGlobalAchievementPercentages`/`GetPlayerAchievements` entirely once it's confirmed the game has zero achievements (a common case), instead of always firing all of them up front. Even when that's unknown, `GetSchemaForGame` is now fetched before (rather than alongside) the rarity/per-account calls, so an empty schema alone is enough to skip them too.
- `getAppDetails` (`lib/steam.js`) now extracts `achievementCount` (Steam's own `achievements.total`) from the store metadata response it already fetches for every game — free, no extra upstream call — backing the short-circuit above.
- `getPlayerSummaries` (`lib/steam.js`) now dedupes in-flight `GetPlayerSummaries` calls per steamid instead of per whole batch, so two overlapping concurrent requests that partially overlap (e.g. one wants `[alice, bob]`, another arrives moments later wanting `[bob, carol]`) never both fetch the shared id.
- Bumped `@vates/data-table-vanilla` from 0.9.0 to 0.10.0, and the direct `@vates/data-table-core` dependency from ^0.8.0 to ^0.10.0 alongside it. No usage changes needed for the vanilla package — the functions this app relies on (`createDataTable`, `syncViewToUrl`, `resetView`) are unaffected; the release drops some vestigial Solid-based exports (`createTableState`, `DataTableView`) this app never used and adds new opt-in `DataTable` methods (`setRowKey`, `setSelectable`, `onSelectionChange`, `getRowId`, `persistView`). The `data-table-core` bump wasn't optional, though: leaving it at ^0.8.0 kept a stale, older copy at the top of `node_modules` — the one `server.js` statically serves at `/vendor/data-table-core` — while `data-table-vanilla` actually needs 0.10.0's `data-table-core`, which broke `library.html` at load time (`doesn't provide an export named: 'reconcileSelection'`) since the served 0.8.0 module lacked that export. Bumping the direct dependency lets npm dedupe to a single 0.10.0 copy.
- Bumped `@vates/data-table-core`/`@vates/data-table-vanilla` from 0.10.0 to 0.11.0. 0.11.0's headline additions are a new `@vates/data-table-solid` package, Vue `v-model` bindings, and a "TableState namespaced into `sort`/`group` sub-objects" breaking change — none of which touch the vanilla adapter's public `createDataTable`/`DataTableInstance` surface this app relies on, which is unchanged. The vanilla-relevant additions (`onSelectionChange`, `getRowId`, post-construction setters, `usePersistence`) are opt-in row-selection/persistence features this app has no current use case for (no bulk actions on games, and view state already persists via URL sync) — left unused rather than wired up speculatively. Two of this app's own hand-rolled `public/library.js` helpers, however, turned out to duplicate functionality core now ships, and were replaced with it:
  - The half-decade ("1-3-10") log-scale bucketing used by the "Review Count" and "Played (h)" group-by columns (`halfDecadeBucket`/`formatHalfDecadeBucket`) now delegates its bucket-boundary math and range formatting to core's new `bucketLogRange`/`formatLogRange` (`{ divisions: [1, 3] }`), instead of hand-rolled `Math.log10` staircase/formatting code. The one thing core's `min` option can't express — a real zero (never played, 0 reviews) needing its own group distinct from the "<1" catch-all a briefly-played game falls into — is kept as a thin wrapper around core's functions rather than reimplementing the staircase itself.
  - `bindGroupBySort` — this app's own hook that auto-sorted a column when it was added to Group by, by subscribing to `onViewChange` and calling `setViewState` — is removed outright rather than just ported to `insertGroupSort`: `@vates/data-table-vanilla`'s own Group dropdown now calls `insertGroupSort` internally the moment a column is toggled into `groupBy`, so the external hook was fully redundant. Verified live (via a scratch table + simulated dropdown clicks) that toggling a second column into Group by now correctly keeps the first column's sort entry and nests the new one after it — `bindGroupBySort`'s own naive version had a latent bug here, silently discarding the first grouped column's sort (and its nesting order) the moment a second one was grouped, since it unconditionally overwrote `sorts` with a single fresh `[{ key, dir }]` entry instead of inserting.
  Picks up 0.11.0's other vanilla bug fixes for free too (column visibility reconciliation on disjoint column swaps, filter checklist/date-tree checkboxes not updating on trusted mouse clicks).
- `getGameSchema` (`lib/steam.js`): its `isUpstream` error message now includes the appid (`Steam schema error 403 for appid 570`) instead of just the status code, matching the sibling `getProtonDbStatus` error — the bare status code alone gave no way to tell from the server log which game triggered a `GetSchemaForGame` failure.
- `getSteamTags` (`lib/steam.js`) now requests up to 30 tags per game (was 20) and shows all of them — the 10-tag display cap inherited unchanged from the old SteamSpy-based code is gone.
- Game tags (`lib/steam.js`) now come from Steam's own store tag data instead of SteamSpy — `getSteamSpyTags` is replaced by `getSteamTags`, which calls the undocumented `IStoreBrowseService/GetItems` (per-app `tagids`, weight-ordered) and `ajaxgetstoretags` (tagid → name, fetched once and cached long-term as `tagnames:all`) endpoints. This matches what the store page itself, SteamDB, and IsThereAnyDeal show, and fixes games SteamSpy had sparse or no tag data for (e.g. niche/recent titles) showing no tags at all in the side panel despite having plenty on Steam. SteamSpy is no longer used anywhere in the app.
- Bumped `@vates/data-table-vanilla` from 0.8.0 to 0.9.0.
- Side panel (`public/panel.js`, shared by both pages): the game description now renders right under the glance grid, ahead of the tag cloud, instead of after it — it's the "what this game is" text and previously got buried under a wall of tag/genre/category chips. It, along with the tag cloud, now sits in the same bordered `.panel-card` box HLTB/News/Achievements already used, instead of being bare unboxed `<div>`s — the whole panel body now reads as one consistent stack of cards rather than two different visual systems glued together.
- Side panel: the Owners section (who in the loaded slot/library owns this game) now renders right after the description, ahead of the HLTB/News/Achievements collapsibles, instead of dead last below all three — on the comparison page in particular, "who owns this" is often the actual reason the panel was opened, and it used to be buried under sections most people never expand. The sticky sub-nav's button order already matched this (Owners listed first); only the actual section order changes to match it.
- Side panel: the Owners section now moved one step further, to right after the tag cloud instead of right after the description — grouping the two "what this game is" blocks (description, then tags/genres/categories/dev-publisher) together ahead of the "who has it" comparison-specific data, rather than splitting them apart. Also now sits in the same bordered `.panel-card` box every other content section uses (it was the one section still rendering as a bare unboxed `<div>`, inconsistent with the description/tag-cloud/HLTB/News/Achievements cards around it).
- Side panel: a DLC entry's "release date · DLC for *Base Game*" line is now one line instead of two stacked ones — this, combined with the "← Back to *Game*" line a DLC-navigation hop also adds above the title, used to make the sticky header (which stays pinned while the rest of the panel scrolls) noticeably tall for exactly that case.
- Side panel: the DLC card's expanded list is now sorted newest-released first, with not-yet-released entries surfaced above everything else — previously it rendered in whatever order Steam's own `meta.dlc` appid list happened to return, which isn't chronological. Not paginated — a base game with a very large catalog (e.g. Stellaris, Sims 4) still shows every resolved entry, just in a more useful order.
- Side panel: the sticky sub-nav (Overview/Owners/HLTB/News/Achievements) now highlights whichever section is actually scrolled into view (`updateSubnavScrollSpy` in `public/panel.js`), instead of being a static row of jump-links with no sense of "where am I" once a panel is long enough to scroll.
- Side panel: the Steam Workshop/Official Website icon-links are now tucked behind a single "⋯ More" toggle instead of each always showing its own icon — with both present (plus the always-there Store/ITAD/copy-link/refresh), the row ran to 6-7 icon buttons, wide enough to squeeze the title into a two-line wrap on a narrow panel (most visible on a DLC entry, which also carries a "← Back" line and a "release date · DLC for X" line above/beside the title). The row's width is now fixed regardless of how many of these a given game happens to have; the "⋯" button itself is only rendered at all when there's at least one to show.
- Side panel: dropped the standalone 📰 News icon-link that used to sit in the header as a fallback for whenever the News card had nothing to show — the card's own ↗ icon (once it renders) already covers the case that matters (linking to the exact same Steam news hub), and a game with confirmed zero official posts isn't worth a dedicated icon just to go check whether unrelated syndicated press showed up on the raw hub instead.
- Site-wide contrast pass: `--text2` (`#4c6b8a`), used for most secondary/caption text (section titles, glance-chip captions, HLTB labels, news meta, achievement descriptions, owner/tag legends, footer links, etc.), measured well under WCAG AA against its actual backgrounds — 2.68:1 against `--bg0` and a failing 1.75:1 against `--bg1` (`--bg1` is the side panel's own background, so most panel captions were the worst-hit case). Every real-text use of `--text2` now uses `--text1` (`#8f98a0`) instead — 5.09:1 against `--bg0`, 3.32:1 against `--bg1` — while the handful of genuinely decorative/exempt uses (input placeholders, a disabled button's label, the offline-status dot's fill) keep the original muted `--text2`, since WCAG doesn't require those to meet text contrast and dimming them further than real content is intentional.
- Side panel (`public/style.css`): the collapsible HLTB/News/Achievements card headers no longer share the exact flat background/chevron look of the (non-interactive) glance grid chips right above them — both reused the same `panel-glance-num`/`panel-glance-val` chip template, so a static data point (e.g. the SteamDB score) and a click-to-expand control looked like the same kind of element. The card background stays `--bg0` (the same fill the glance chips already use, with proven ~1.5:1 contrast against the panel's own `--bg1`, rather than `--bg1` itself, which measures ~1:1 against the panel and made the cards nearly invisible but for their border), plus a 3px accent-colored left stripe and a chevron that's accent-colored at rest, not just on hover — so "this expands" reads as a control at a glance, without losing visibility.
- Side panel (`public/panel.js`, shared by both pages): the tag/genre/category/developer-publisher cloud now renders right after the glance grid instead of at the very bottom of the panel, below the (collapsed) HLTB/News/Achievements cards and the owner list. That identity info — what kind of game this is — is something most people want within the first screenful, especially when opening an unfamiliar title from the comparison page; the description (marketing blurb) now follows it instead of preceding it.
- Side panel: the title row's 📰 icon-link (out to the game's Steam news hub) is now only shown when the News card itself isn't rendered — once the News card exists, its own ↗ link already points at that exact same URL, so showing both was a plain duplicate. The icon still appears as a fallback for a game with no fetched headlines (no News card to carry a link at all). The HLTB and News cards also gained a leading icon (⏱️/📰, the same 📰 retired from the title row) in the numeral's slot — both cards had no number to show there (see `collapsibleCard`'s `numHtml` doc), which left them reading as plain text next to Achievements' bold colored percentage instead of as one visual family of cards.
- Side panel: the game description now reads at the same primary-text weight as the title and tag names (`--text0`, 13px) instead of the muted secondary-caption treatment (`--text1`, 12px) it shared with things like the HLTB labels and tag legend around it — it's prose meant to be read, not metadata, and was blending into both neighbors at that weight.
- Library Explorer: the Wishlist tab's "Wishlist Rank" column is now hidden by default (still available via the column picker) — Steam's wishlist ordering isn't something most users track or find meaningful at a glance.
- Library Explorer: the "Last Played" column is now hidden by default too (still available via the column picker). Steam's `GetOwnedGames` only returns real `rtime_last_played` data for the account whose own API key is querying it — every other account comes back with it silently absent, indistinguishable from "owned but never launched." Most searches here involve at least one non-key-owner account, so showing it by default risked reading as real data when it's often just an unavailable field. The column header now also gets a tooltip warning specifically when every currently loaded game shows no value at all — the actual tell that the whole column is unavailable for this account, rather than that nobody's touched their library.
- Both pages now write the shareable `?u=` URL and the localStorage recent-searches entry from the server-resolved Steam64 ID(s), not the raw typed input. Previously a vanity name (or a raw ID) was written verbatim as soon as the search was submitted, so the same player could produce differently-spelled URLs/recents depending on what was typed, and a vanity name that its owner later changed would leave a stale, no-longer-resolvable string behind. The write is now deferred until the search's fetch actually resolves the IDs (`public/app.js`'s `findCommonGames`, `public/library.js`'s `loadLibrary`/`loadWishlist`) rather than happening optimistically beforehand — simpler than a write-then-correct scheme, at the cost of the URL/recents no longer updating instantly while a search is still in flight, and not updating at all if it fails. `game`/`shot` deep-link params still clear immediately on a genuine new search, since they don't depend on anything the server resolves.
- The `[game-details]` upstream-failure warnings logged by `fetchGameDetails` (`server.js`) now include the appid — with several games fetched in parallel via `/api/game-details/stream`, a bare `ProtonDB error 403` gave no way to tell a single game's failure apart from every game in the batch failing the same way. The ProtonDB error message itself also now carries the appid plus a short response-body snippet, since that endpoint is called with no headers at all (unlike HLTB's spoofed UA/Referer) and a 403 there is as likely to be a blanket bot-block as a per-game issue. Those same warn lines now also print `err.cause` when present — Node's `fetch` throws a generic `fetch failed` `TypeError` for any network-level failure (DNS, connection reset, timeout, ...) and buries the actual reason there, so without it a whole batch of failures looked identical with no signal about the real cause.

### Fixed

- Shared game panel (`panel.js`, used by all three pages): the close button and prev/next/random nav buttons' mobile-specific enlargement (`@media (max-width: 768px)`) was dead code — a later, unconditional `.panel-close`/`.panel-nav-btn` rule further down `style.css` won the cascade at equal specificity regardless of viewport, same source-order trap as the region-picker bug below. Confirmed live at 360px: the close button rendered 28×28 and the nav buttons 36×32, both under the ~44px recommended minimum touch target. Fixed by moving each mobile override to sit directly after its own base rule instead of a shared faraway block, so this can't silently drift out of sync again.
- Library Explorer: the "Prices in" region picker was visible on the Library tab even though `library.js` already sets its `hidden` property to true there — `.wishlist-region-label`'s own unconditional `display: flex` was overriding the browser's default `[hidden]{display:none}`, since author CSS always wins over the UA stylesheet regardless of specificity. Confirmed live at both mobile and desktop widths. Added an explicit `.wishlist-region-label[hidden]{display:none}` rule.
- Library Explorer: the tabs row (`.lib-tabs`, holding the Library/Wishlist toggle and — on the Wishlist tab — the region picker) and the load form row (`.lib-form`, input + Load Library/Load Wishlist button) had no `flex-wrap`, so at phone widths (confirmed at 360px) the page overflowed horizontally and the Load button was clipped off-screen entirely. Both rows now wrap under `@media (max-width: 768px)`; desktop layout is unchanged.
- Bundles page: each bundle-list row's price, game count, and expiry date were pushed off-screen at phone widths (`flex-wrap: nowrap`) with no visible indication that scrolling the row horizontally would reveal them. Now wraps onto a second line under the title at ≤768px instead, so every field is visible without scrolling.
- Lightbox: Shift+Left/Right (the "force media navigation over a playing video" escape hatch) silently did nothing while focus was on the video's scrub bar — the native range-input's own arrow-key handling nudged its value instead, since the keyboard handler skipped arrow keys entirely whenever focus was on the scrub bar. Shift+arrow now steps the lightbox regardless of focus, as intended; a bare arrow on the scrub bar still operates the slider.
- Per-request log spam once `steam-store`'s circuit breaker is open: every blocked request used to log its own `[upstream:...]`/`[game-details]` line repeating the same already-explained "circuit open" outcome — potentially hundreds of near-identical lines during a large batch load. The underlying error is now marked `isCircuitOpen: true` at its single throw site, and both logging call sites (`server.js`'s `routeErrorStatus` and `fetchGameDetails`'s `logErr`) skip logging it (status codes/null fields are unaffected).
- Library Explorer Wishlist tab: the "Added" column (`dateAdded`) was `groupable: true` with no year bucketing, unlike Released/Last Played right next to it — grouping by it produced close to one group per day instead of a useful year split. Now grouped by year via `bucketDatePart('year')`, same as the other two date columns.
- `lib/hltb.js`'s per-search `"[HLTB] search failed"` warning had no backoff, unlike the auth-init failure path — a systemic HLTB outage mid-search (auth still valid, search endpoint itself rejecting everything) would log once per game in whatever library/comparison is loading. Now throttled to once per 30s window, same as the existing auth-retry cooldown; the searches themselves are unaffected, only how often the warning prints.
- Bundles page and the Library Explorer's Wishlist tab: a page-level "↻ Refresh prices" button (next to Reset view) force-refreshes every price column for whatever's currently loaded in one batched `POST /api/prices` call, bypassing the `itad-price:` cache — previously the only way to get fresh ITAD pricing for a game was the side panel's own "↻ Refresh" button, one game at a time, which was needlessly slow given pricing is already fetched as a single cheap batch call.
- Best Deal cell (`renderBestDeal`, both `bundles.js` and `library.js`) and the side panel's Price card (`priceHtml` in `panel.js`) now also recognize a 3-month low (amber + ☆, same treatment as the 🔥/★ tiers) — they previously only checked all-time/1yr low, so a game whose Price Status column read "3mo Low" showed no visual indicator at all on the Best Deal price itself or in the panel.
- Bundles page: pressing Escape while the lightbox was open did nothing instead of closing it — the handler only ever checked whether the lightbox was open to decide *not* to close the panel behind it, but never actually closed the lightbox itself.
- Bundles: Steam Full Price/Best Deal/All-Time Low/1yr Low/3mo Low could silently render in the wrong currency — right number, wrong symbol — for a bundle whose own tier price comes back from ITAD in a different currency than its games' Steam/other-shop prices do (confirmed live: a Humble Bundle-published bundle priced in USD-only on ITAD regardless of region, whose games' own Steam/other-shop prices correctly came back in the requested EUR). All five columns previously shared one `row.currency` field, populated first by the bundle's own tier price and never overwritten afterward; split into `tierCurrency` (backs only the Tier Price column) and `priceCurrency` (backs the other five, always set directly from the `/games/prices/v3` response that produced them) so the two can legitimately disagree without either mislabeling the other's amounts.
- Bundles: changing the region dropdown while a bundle was open didn't actually refresh that bundle's prices — it only reloaded the browse list above, leaving the open bundle's Tier Price/Steam Full Price/Best Deal/lows columns stuck on whatever country was previously selected. The region change now also re-opens the currently active bundle (same `GET /api/bundles/:id` fetch a `?bundle=` deep link uses), so its prices actually pick up the new country.
- Bundles: the bundle list visibly popped open and immediately snapped shut again — most noticeable loading the page with a `?bundle=` deep link, or changing region while a bundle was open — because `loadBundles()`'s own unconditional re-expand and the bundle-(re)open's own unconditional re-collapse (`setListCollapsed`) ran concurrently with no coordination between them, so whichever landed second "won" for a moment before the other caught up. `loadBundles` now takes an `expandList` option that both call sites pass `false` when a bundle-open is about to follow right after and will handle the collapsed state itself; a deep link that turns out not to resolve to a real bundle now also explicitly re-expands the list instead of leaving it stuck collapsed with nothing open underneath it.
- Bundles: the "Most games" and "Best discount" sort options 400ed against the real API ("Invalid 'sort' value") the moment either was picked. Confirmed live against the upstream API (ITAD's docs list `sort` as a bare, un-enumerated string) that the correct key for bundle size is `-games`, not `-count` — fixed. There is no discount/cut-based sort for bundles at all (a bundle has per-tier prices, not one discount value; isthereanydeal.com's own bundle list sort dropdown doesn't offer one either) — "Best discount" is removed from the dropdown rather than pointed at a working value that doesn't exist.
- Bundles: the auto-detected region defaulted to the US far more often than it should — `detectCountry()` relied solely on `navigator.language`, which reflects the browser/OS's UI language preference, not physical location, and very commonly stays "en-US" (or another English variant) regardless of where the user actually is (confirmed live: a machine with `LANG=en_US.UTF-8` but an OS timezone of `Europe/Paris`, which resolved straight to "US"). Also confirmed live: even a correctly-French `navigator.language` wouldn't have helped, since detection only ever accepted an *exact* match against the curated `COUNTRY_OPTIONS` list and `FR` isn't one of its entries — the "Germany / EU (EUR)" bucket's intent to stand in for the whole Eurozone was never actually implemented. `detectCountry()` now tries the OS timezone first (`Intl.DateTimeFormat().resolvedOptions().timeZone`) via a new curated `TIMEZONE_COUNTRY` map — a stronger real-location signal — including a Eurozone-country-to-`DE` mapping, before falling back to the language-based guess.
- HLTB lookups (`lib/hltb.js`) were failing with `HLTB auth unavailable`/`404` on every request — HowLongToBeat renamed its undocumented auth/search endpoints from `api/bleed/init`/`api/bleed` to `api/search/site/init`/`api/search/site`. Updated to the new paths (payload/header shape is unchanged).
- That same rename left test mocks in `test/hltb.test.js`/`test/server.test.js`/`test/server-ratelimit.test.js` still targeting the old `api/bleed`/`api/bleed/init` paths — the fix above updated `lib/hltb.js`/`CLAUDE.md` but not these mocks, so every test exercising HLTB (18 in total) failed with "Unexpected fetch" until they were updated to match.
- HLTB times (`main`/`extra`/`completionist`/`all`, `lib/hltb.js`) are now rounded to the nearest **half** hour instead of a whole hour, matching the granularity HLTB's own site displays (e.g. "2½ Hours"). Previously the decimal shown in the Library Explorer's How Long To Beat columns and the side panel (e.g. "2.0h") was purely cosmetic formatting — the underlying value had already been rounded to a whole hour server-side, so the ".0"/".5" never reflected real precision.
- Library Explorer: grouping by "Played (h)" sorted the "Not played" group between "1-3h" and "<1h" instead of last (when sorted most-to-least played) — the "<1h" bucket's sentinel value was `-1`, which is numerically *less* than the real-zero "Not played" bucket, so "played a little" ranked below "never played". Changed to `0.5` (strictly between the "Not played" bucket's `0` and the first real decade bucket's `1`), which keeps `0 < 0.5 < 1 < 3 < ...` correct in both sort directions.
- `getTagNameMap` (`lib/steam.js`): `ajaxgetstoretags` was observed returning a `200 OK` with a literal JSON `null` body (instead of the usual `{tags: [...]}`) during what looked like an upstream hiccup — destructuring `tags` straight off that threw an opaque `TypeError` ("Cannot destructure property 'tags' of ... as it is null") instead of a clean, caller-recognizable upstream error. The response body is now checked before destructuring; an unexpected shape now throws a normal `isUpstream` error and isn't cached, so the next call retries instead of getting stuck.
- `getSteamTags` (`lib/steam.js`): the cache key for a game's tags is now `tagids:{appid}` instead of reusing the old SteamSpy-era `tags:{appid}` key. That key's stored shape changed from a `{tagname: voteCount}` object (SteamSpy) to a `tagid[]` array (Steam's own tag ids, see the tags source swap above); a pre-existing `tags:` entry from before this change was read back by the new code as-is and crashed on `tagIds.slice is not a function` the first time an already-cached appid's tags were requested. An initial fix treated any non-array cached value as a miss, which handled that crash but missed a quieter case: a game with zero SteamSpy votes had cached a bare `[]` under the old key, indistinguishable by shape alone from a legitimate empty result under the new format — those games stayed permanently stuck showing no tags even once Steam's own data had some. Moving to a distinct key prefix sidesteps the ambiguity entirely; old `tags:` entries are never read again and age out on their own via the existing 60-day meta TTL sweep.
- Side panel (`public/panel.js`, shared by both pages): the News and Achievements sections used to render nothing at all when their fetch failed, looking identical to "this game genuinely has no news/achievements" instead of a failure worth noticing. Both now show an explicit "Couldn't load …" message when there's no data to fall back on (a failed *refresh* that still has stale data from an earlier successful load keeps showing that stale data instead, rather than replacing it with an error).
- Side panel: clicking a DLC entry's "Part of *Base Game*" link right back to the base game you just came from (or vice versa) used to push a duplicate consecutive entry onto the DLC-navigation history stack (`panelHistory`) instead of just going back — `navigateToGame` now collapses that case by popping the stack instead of growing it.
- Side panel (`public/panel.js`, shared by both pages): expanding a base game's DLC card no longer holds the whole card on its loading skeleton until *every* DLC entry has resolved — a franchise with a large DLC catalog (e.g. Stellaris, 30+ entries) could take a long time to show anything at all, since one slow/rate-limited entry blocked the rest from appearing even though most had already loaded. `loadDlc` now fills a `game.dlcPartial` slot as each entry resolves and re-renders immediately, so already-loaded entries appear right away with a "Loading N more…" line for what's still pending; a force-refresh seeds those slots from the previous complete list so the card doesn't visibly shrink back to empty while refreshing.
- Side panel (`public/panel.js`, shared by both pages): expanding or collapsing an HLTB/News/Achievements card (`collapsibleCard()`) no longer yanks the whole panel's scroll position back to the top. `renderPanelBody` rebuilds `#panel-hero` from scratch on every re-render, and `buildPanelHero()` unconditionally `scrollIntoView()`s the active filmstrip item — appropriate when actually opening the panel or paging the hero, but it fired just as eagerly on a re-render that had nothing to do with the hero, scrolling the reader back up to it. The panel body's scroll position is now snapshotted before re-rendering and restored after.
- Comparison page (`public/app.js`): a standalone "look up any game" lookup (no player loaded, or a game nobody in the comparison owns) now shows the Achievements section too, matching what the Library Explorer already did for its own no-player case — the section was silently never wired up on this page at all (`showAchievements` was never set on `initPanel`), so it just never appeared regardless of whether a game had achievements. A loaded comparison-group game still shows nothing here, same as before: unlike the Library Explorer's single player, a comparison group has no one well-defined account to fetch unlock progress for (a game can belong to several slots/Families at once), so per-account progress on this page remains a separate, not-yet-tackled piece of work — this fix only covers the list-only, no-progress case standalone lookups already support server-side (`steamids` is optional on `GET /api/achievements/:appid`).
- `getAppDetails` (`lib/steam.js`): a game's tag cloud / Categories & Genres columns could show the exact same pill twice — Steam's own store API sometimes lists two distinct category/genre ids that share an identical label (e.g. app 3357650 has separate ids for "DualShock Controller Support" and separate ids for "DualSense Controller Support", each pair rendering the same text). Since only the label is ever displayed, never the id, `genres`/`categories` are now deduped by description at the source rather than in every place that renders them.
- `server.js`: the five route handlers with a plain try/catch (`/api/common-games`, `/api/wishlist`, `/api/search-games`, `/api/game-news/:appid`, `/api/achievements/:appid`) only ever logged an `isUpstream` or `TimeoutError` failure — anything else, including a genuine unhandled bug (a `TypeError`, a bug in `groupByOwnership`, etc.), fell through with no server-side log at all, since a plain unmarked `Error` was indistinguishable from one of `lib/steam.js`'s deliberately-thrown, expected client-facing errors (bad Steam id, private profile). Those two call sites now throw a `clientError()` (mirroring the existing `upstreamError()`), and every route funnels its catch through a shared `routeErrorStatus()` that skips logging only `isClientError` failures — an upstream error, a timeout, or anything unmarked (assumed to be a bug) is now always logged, with the full stack rather than just the message. The SSE batch route (`POST /api/game-details/stream`) had one fully silent `catch` around each per-game fetch (a defensive backstop that shouldn't fire in practice, since `fetchGameDetails` resolves everything via `Promise.allSettled` internally) — it now logs too instead of vanishing without a trace if it ever does.
- Side panel glance strip (`glanceGrid`, `public/panel.js`): the SteamDB, Metacritic, and Linux/Deck (ProtonDB) chips no longer disappear when that source has no data for a game (no reviews, no critic score, no ProtonDB reports) — they now render a "—" placeholder with a "no rating"/"no score"/"no reports" caption instead, matching the fallback the HLTB chip already had. A missing chip used to collapse the fixed 2×2 grid into a lopsided 3-chip layout and silently hid the fact that data was missing rather than absent from the panel entirely.
- Side panel HLTB chip: a game HLTB has matched but nobody has submitted completion times for (`h.all` null, e.g. "The Expanse: Osiris Reborn") now links to that game's real HLTB page (`h.id`, from `pickBestMatch` in `lib/hltb.js`, which returns `id` independently of `all`) instead of falling back to a generic HLTB search — the search fallback is now reserved for when HLTB has no match at all.
- Side panel: the hero image/filmstrip and glance strip no longer permanently occupy space above the scrollable body — on short/mobile screens they (plus the sticky title bar) could eat most or all of the visible panel before any real content (description, HLTB breakdown, owners, tags) came into view, with no way to scroll them out of the way. The hero is now rendered as the first child of the scrollable `#panel-body` (`renderPanelBody`, `public/panel.js`) instead of a fixed sibling outside it, and `.panel-header-sticky` now wraps only the title row (title, release date, the store/IsThereAnyDeal/refresh icon links) — the glance grid moved out to scroll away with the hero. Listeners that used to bind directly to `#panel-hero` (click/keydown/wheel in `initPanel`, touch handlers in `initHeroSwipe`) now delegate off the stable `#panel-body` instead, since the hero element itself is recreated on every render. The glance grid's leftover margins (inherited from when it sat inside `.panel-header-sticky`) also made the gap above it nearly 3x the gap below — now symmetric.
- Side panel on landscape mobile: the hero's height used to scale purely off the panel's width via `aspect-ratio` — fine in portrait, where width is the panel's scarce dimension, but on a wide/short landscape viewport (where the panel intentionally still goes full-width, see `.game-panel`'s mobile rule) that produced a hero tall enough to dominate the whole screen before any content, including the sticky title bar, came into view. `.panel-hero-main` now gets a `max-height: 30vh` cap under `@media (max-height: 500px)`, keyed on height rather than width/orientation so it also catches landscape phones wide enough to already dodge the `max-width: 768px` mobile rule.
- Library Explorer wishlist: the Released column's range filter/slider and date-tree dropdown no longer treat "Coming soon"/"TBA" placeholders as real dates. `endOfReleasePeriod` (the column's `parseDate`, also driving the framework's range-filter bounds and date-tree grouping — not just sorting) used to anchor those placeholders at a sentinel timestamp in the year 9999 so they'd sort deterministically after every real date; that same fake timestamp leaked into the other consumers of `parseDate`, dragging the whole column's computed max to year 9999 (squashing every real release date into a sliver of the range slider), letting a "released after X" filter wrongly match games with no known release date, and growing a nonsensical "9999" branch in the date-tree. The sentinel logic moved into a new sort-only helper, `releaseSortTimestamp` (used solely by the `compareDateMissingLast` sort comparator); `endOfReleasePeriod`/`parseDate` now treats those placeholders as unparseable (`NaN`), same as any other unrecognized string, so the range filter, slider bounds, and date-tree all correctly treat "unreleased" as "no known date."
- Side panel: a broken hero image (e.g. a video whose poster/thumbnail URL 404s upstream on Steam's own CDN — the video itself, played via the lightbox, can still work fine) no longer hides the entire hero. `setupHeroImg` (`public/panel.js`) used to respond to the `<img>` failing to load by hiding `.panel-hero-main` outright, which also took down the prev/next nav and, for a video, the play-button overlay and click target — the whole hero pane vanished instead of just the broken thumbnail. It now only marks the image itself broken (`.panel-hero-img--broken`) and leaves the rest of the hero usable. The filmstrip thumbnails got the same treatment, plus a fallback: a broken screenshot *thumb* now retries with the full-res image behind it (more likely to still be alive than the small thumbnail variant) before giving up.
- Side panel: on touch devices, dragging right over the hero filmstrip to scroll it toward earlier thumbnails no longer gets hijacked by the swipe-to-close gesture. `initPanelSwipe` (`public/panel.js`) tracked touches starting anywhere in the panel and `preventDefault()`'d any rightward horizontal drag to animate the close, which blocked the filmstrip's own native scroll for that same direction; it now excludes touches starting on `.panel-filmstrip`, matching the exclusion `initHeroSwipe` already had for its own hero-image-stepping swipe.
- ProtonDB requests (`getProtonDbStatus`, `lib/steam.js`) had no concurrency limiter, unlike Steam's store fetches or SteamSpy — a library/comparison load fetching details for dozens of games at once fired that many unthrottled connections at `protondb.com` simultaneously, which surfaced as bursts of network-level `fetch failed` errors (connection resets/DNS contention) across most or all of the games in the batch. Now capped at 3 concurrent requests, same as SteamSpy.
- An unreleased game with an announced release date (e.g. "Oct 14, 2026") no longer shows a blank Released date in the wishlist/side panel. `getAppDetails` (`lib/steam.js`) was nulling out the date whenever Steam's `coming_soon` flag was true, conflating "not out yet" with "no known date" — it now always passes through whatever date string Steam provides.
- Library Explorer: sorting/filtering by Released no longer misorders games with a coarse or fuzzy date ("2026", "Fall 2026", "Q4 2026", "2026 or later") — `new Date()` anchors those at the *start* of the period it can parse (e.g. "2026" → Jan 1), which could sort a still-unreleased game before ones that already shipped earlier that same year. A new `endOfReleasePeriod` helper anchors coarse dates at the *end* of their stated period instead (the latest point consistent with what Steam told us), and is now wired into both the Released column's sort `compare` and its `parseDate` (so the date filter tree/range picker agree with the sort order too, which they didn't before since `parseDate` wasn't set at all).
- Library Explorer: refined that same Released ordering further — a bare year and Q4 of that year used to tie exactly (both anchor at Dec 31), so `endOfReleasePeriod` now nudges the less-specific bare year 1ms later (e.g. `Q3 2026 < Q4 2026 < 2026 < 2028`). "Coming soon" and "To be announced"/"TBA" are also no longer unparseable placeholders dumped in an unordered bucket at the very end — they now get real sentinel positions after every dated/coarse entry, in that order (Coming soon carries at least some signal that TBA doesn't).

### Security

- Side panel (`public/panel.js`): a game's store `short_description` was interpolated directly into `panelBody.innerHTML` with no escaping (every other field in the same template went through `esc()`), and the panel opens for any appid with no ownership check — a malicious/compromised Steam listing's description could run arbitrary script in the app's origin the moment its panel opened. `esc()` alone wasn't the right fix since the field can legitimately contain literal HTML entities (e.g. "Baldur's Gate... Dungeons &amp; Dragons") that `esc()` would double-escape into visible `&amp;` text. It's now decoded via an inert `DOMParser` document (no scripts run, no resources load) and inserted with `textContent` instead.
- Side panel: the Official Website link and news-item links used only `esc()` on their `href`, which escapes HTML special characters but not the URL scheme — a `javascript:`/`data:` URL from a developer-supplied website field or a news item would have passed through untouched. Both now go through the same `/^https?:\/\//i` allowlist `accountsBar.js`/`app.js` already apply to profile URLs (`safeHref` in `panel.js`).
- `POST /api/game-details/stream`: unlike every sibling data-fetching route, this SSE batch endpoint had no rate limit and no cap on how many games one request could list — a single request could queue unbounded upstream Steam/HLTB/SteamSpy/ProtonDB calls and unbounded `db.sqlite` cache writes. It now shares `detailsLimit`'s per-minute budget and rejects a request above the new `STREAM_MAX_GAMES` cap (default 5000).
- `lib/steam.js`: the shared upstream-call semaphores (`storeLimit`/`spyLimit`/`protonLimit`) had no maximum queue depth — `createSemaphore` now takes a `maxQueue` (20000) as a coarse safety valve against unbounded queue growth, on top of the request-level bound above.

## [0.3.0] - 2026-08-14

### Added

- "Look up any game" search box on both pages (new shared `public/gameSearch.js`, new `GET /api/search-games` wrapping Steam's undocumented store search endpoint): type a name to pick from a dropdown of matches, or paste a bare appid/store URL directly, and preview it in the existing side panel — score, HLTB, tags, screenshots, links — regardless of whether anyone in the current search owns or has wishlisted it. `GET /api/game-details/:appid` already had no ownership check, so this only needed a way to turn a typed name into an appid. On the Library Explorer, the `?game=<appid>` deep link (see below) now also falls back to fetching an unmatched appid directly instead of silently doing nothing, including with no player loaded at all — the panel shows a placeholder title until the fetch resolves the real one (see "Changed" below for why the name isn't carried in the URL).
- Library Explorer: opening a game's side panel now writes `?game=<appid>` to the URL, and opening the lightbox on top of it adds `&shot=<idx>`, mirroring the comparison page. A copied link reopens the same game/media once the page's data has loaded. A genuine new Load/refresh/tab-switch clears these params instead, since a game left open from a previous player/tab may not exist in the new list.
- Comparison page: a looked-up game now also writes `?game=<appid>` (and `&shot=<idx>`) to the URL, and restores it on load the same way the Library Explorer already did — including falling back to fetching the appid directly when it isn't part of the loaded comparison, or nothing is loaded at all. Previously the comparison page deliberately skipped writing this param since it had no way to restore it, so a direct game lookup on that page was lost on refresh/reload.
- A "Recently looked up" row of games below the "look up any game" box on both pages, backed by one `localStorage` list shared across both pages (unlike the player-search "Recent:" row, which is namespaced per page) — clicking an entry reopens that game in the side panel. Capped at 10, individually removable, plus a "Clear" for the whole list.
- Comparison page: each slot's account chips now render in their own bordered box, labelled `Player N` once there's more than one slot and/or `N accounts merged` whenever a slot itself unions more than one account (a Steam Family). Previously a multi-slot search where every slot was itself a multi-account merge (e.g. two Families being compared) rendered as one undifferentiated flow of chips with small "Player N" labels in between, with no visual cue for which chips were merged together versus being compared side by side.
- ProtonDB Linux/Steam Deck compatibility tier, shown as a colored badge in the side panel (new "Linux / Steam Deck" section, linking out to the game's ProtonDB page) and as a new hidden-by-default "ProtonDB" column in the Library Explorer table, sorted/grouped/filtered by actual compatibility quality (Borked → Bronze → Silver → Gold → Platinum → Native) via `@vates/data-table-core`'s new per-column `compare` option (see "Changed" below) rather than alphabetically, with unrated games always pinned to the end of the sort regardless of direction (`compareMissingLast`). Backed by a new `getProtonDbStatus` (`lib/steam.js`) wrapping ProtonDB's undocumented summary API (same trust tier as the HLTB/wishlist endpoints — see CLAUDE.md's compliance notes), cached under a new `protondb:` prefix on the existing 60-day `meta` cache tier (tags/HLTB/store-metadata's tier — Proton compatibility reports don't change fast either). Wired into both `GET /api/game-details/:appid` and the `POST /api/game-details/stream` SSE endpoint alongside rating/HLTB/meta/tags. ProtonDB's "pending" tier (too few reports to confidently rate — not a compatibility outcome) is treated as no rating at all rather than shown, since it isn't a point on the same quality scale as the other tiers.
- The "look up any game" dropdown (`public/gameSearch.js`, both pages) is now keyboard-navigable: `↑`/`↓` moves a highlight through the results (wrapping at both ends), `Enter` picks the highlighted result (falling back to the top match if none is highlighted yet, as before), and `Escape` still closes it. Added proper combobox ARIA wiring (`role="listbox"`/`"option"`, `aria-activedescendant`, `aria-expanded`) — previously arrow keys did nothing and Enter could only ever pick the very first result.

### Fixed

- Library Explorer: pasting a full profile URL (e.g. `https://steamcommunity.com/id/name` or `.../profiles/7656...`) into the player input now resolves correctly, matching the comparison page. `library.js` was passing the raw comma-split input straight to `/api/common-games`/`/api/wishlist` instead of running each member through `normalizeInput()` (shared `public/utils.js`, already loaded on the page) to strip a pasted URL down to the vanity name or Steam64 ID first.
- Lightbox: the video scrubber/timestamp no longer briefly show the previous video's playhead position when switching to a new video — they're now reset to 0:00 immediately on load instead of waiting for the new `<video>`'s own `timeupdate`/`durationchange` events to catch up.
- Library Explorer: the filter dropdown's value checklist (e.g. Genres) no longer leaves blank space below it when the panel is stretched taller by the dimension list on the left — `@vates/data-table-vanilla` gives that checklist a fixed pixel height regardless of the panel's actual rendered height, so `library.html` now overrides it to flex and fill the available space instead.
- Both pages: closing the game detail side panel via the backdrop click, × button, or swipe-to-close now clears `?game=`/`&shot=` from the URL, same as pressing Escape already did. Those three paths call `panel.js`'s `panelClose()` directly rather than through either host page's own close wrapper, so they used to leave the URL (and, on the comparison page, its `activeGame` tracking and the table's active-row highlight) stale until something else happened to reset them. `panel.js` now exposes an `onClose` option that runs on every close path, and `panelClose({ preserveUrl: true })` for the one case that needs to close-then-immediately-reopen the same game without losing its `?game=` deep link (e.g. a per-account "↻" refresh while a panel is open).
- Comparison page: a recent-search entry for two merged Steam Families (e.g. two 2-account slots) no longer displays identically to a plain 4-player comparison — both used to flatten to the same "Player A + Player B + Player C + Player D" label. The stored snapshot now keeps one array per slot, so the label joins accounts within a slot with "+" and separates slots with ",", matching the accounts bar's own "N accounts merged" wording.
- Side panel: the hero banner image no longer fails to load (and stay broken no matter how many times the panel is closed and reopened) for games Steam has moved to a newer CDN asset pipeline. It was always guessing a conventional `.../apps/<appid>/header.jpg` path instead of using the `header_image` URL Steam's own store metadata already provides for that specific game — a deterministic 404 for any game whose real header isn't at that guessed path, not a transient glitch a retry could fix. `getAppDetails` (`lib/steam.js`) now returns `banner` (`header_image`, falling back to the guessed path only when Steam's metadata omits it) and `public/mediaItems.js` uses it once metadata has loaded.
- Comparison page: refreshing a game's details from the side panel's "↻" no longer makes the (otherwise-hidden) results-table Filter card spuriously appear when the open game is a standalone "look up any game" lookup rather than a row from a loaded comparison. `refreshGameDetails` was feeding the refreshed game's tags/genres/categories into the results table's filter-option pool unconditionally; it now skips that for standalone lookups, matching how the rest of the app already treats them as not part of any loaded group. Also fixed the standalone lookup's initial fetch and the normal per-row streaming path both silently dropping the new `protondb` field from `game.details` (see "Added" above) on every refresh/reload.
- Library Explorer: sorting by SteamDB Rating, Wilson Score, Steam %, Metacritic Score, or any of the four HLTB columns no longer treats a game with no data there the same as one confirmed to score/last literally zero. `type: 'number'` columns coerce their value via `Number(v)` for sorting, and `Number(null)` is `0` — so a game with no reviews at all used to sort identically to one confirmed 0% positive (worst possible, indistinguishable from "unrated"), most visibly on SteamDB Rating, the table's default sort column. Same issue on Released (and the Wishlist tab's Added): a `null` date coerced to the Unix epoch via `new Date(null).getTime()`, sorting an unreleased/unknown-date game as if it were ancient. All six now use the new `compare`/`compareMissingLast` from the `@vates/data-table-core` bump below, so missing data sorts last regardless of direction instead of silently impersonating a real value. Review Count and Played (h) were left alone — a real `0` there is meaningful data, not a stand-in for "unknown."

### Changed

- Lightbox: while a video is playing, bare `←`/`→` now seek it ±5s in place instead of jumping to the previous/next screenshot, so scrubbing through a clip no longer accidentally navigates away from it. `Shift+←`/`Shift+→` still forces media navigation during playback. Behavior for images is unchanged. Shortcut legends on both pages updated to match.
- Library Explorer: rows no longer appear in the table before their details (score, HLTB, genres, etc.) have streamed in — previously every row showed up immediately with '…' placeholders in most columns and filled in progressively as `/api/game-details/stream` delivered each game. Rows now stay out of the table entirely until loaded, then pop in already fully populated; the "`X` / `Y` games loaded…" status text still tracks progress in the meantime. Prev/next panel navigation and random-pick now also only consider loaded rows, matching what's visible.
- `GET /api/game-details/:appid` and `POST /api/game-details/stream` no longer accept a `name` query param/field at all — the server always resolves a game's name itself from store metadata, keyed on the appid, the same appid-keyed source used for everything else about the game. This endpoint has no ownership check, so a client-supplied name was never anything more than an unverified string; the only effect it had was letting the HLTB search start in parallel with the store-metadata fetch instead of waiting for it. That's the trade made here: HLTB search always waits on store metadata now, in exchange for the appid being the only thing about a game this app ever trusts from a request. On the frontend, a name known client-side (a "look up any game" dropdown pick) is used only to avoid a title flash in the panel while its own fetch is in flight — it's never sent to the server or written to the URL, so the Library Explorer's `?game=<appid>` deep link for a not-yet-loaded game no longer carries a `gname` param either.
- Bumped `@vates/data-table-core` and `@vates/data-table-vanilla` from 0.5.0 to 0.6.0. The library now natively fills the filter dropdown's value checklist to the panel's real height instead of using a fixed pixel height, so the `library.html` CSS workaround for that (the "Fixed" entry above about blank space below the checklist) is now redundant and has been removed — verified visually that the checklist still fills correctly without it. Also picks up a new slider control on numeric/date range filters, which reuses existing dark-theme color variables (`--color-border-secondary`, `--color-text-info`) already overridden in `library.html`, so no new theme CSS was needed.
- Bumped `@vates/data-table-core` and `@vates/data-table-vanilla` again, 0.6.0 → 0.7.0, for a new per-column `compare?: (a, b, dir) => number` option (and the `compareMissingLast` helper built on it) — filed as [vatesfr/data-table#15](https://github.com/vatesfr/data-table/issues/15) while building the ProtonDB column above, which needed to sort by compatibility quality rather than alphabetically. An earlier version of that column worked around not having this by baking a rank digit into the stored value itself ("3 Gold"), which leaked into the filter checklist as a visible prefix; `compare` replaced that with a real comparator, letting the column go back to a plain "Gold"/"Platinum" value everywhere.
- Library Explorer's page chrome now matches index.html's look instead of its own bespoke, unboxed layout: the header gets the same centered title + tagline treatment (with the "← Comparison tool" back-link repositioned to sit above it rather than inline), the tab toggle + player-load form and the "look up a game" box are now each wrapped in the shared `.card` panel style (with a heading/subtitle on the lookup card, matching index.html's), and the `Library`/`Wishlist` tab buttons now reuse the shared `.btn` class for their base padding/font/border-radius instead of redefining it. `.lib-page` keeps its own wider 1600px max-width (intentional, for the data table) and the data-table color-variable overrides are unchanged.
- Bumped `@vates/data-table-core` and `@vates/data-table-vanilla` again, 0.7.0 → 0.8.0. Core split its injected theme CSS out into a new `@vates/data-table-core/theme` subpath, which the vanilla bundle now imports as a bare specifier — `library.html`'s importmap needed a matching new entry (`/vendor/data-table-core/theme.js`, already served since the whole `dist/` folder is statically mounted) or the page's module script threw `Failed to resolve module specifier` and the table never rendered; verified fixed by loading a real library and exercising sort/group/filter with no console errors. The new `@vates/data-table-core` also adds `getSelection()`/`setSelection(rows)`/`clearSelection()` instance methods for programmatic row-selection control, on top of the existing `selectable`/`onSelectionChange` option — not adopted yet, since neither page currently turns on row selection.
- Library Explorer: adopted 0.8.0's two other new per-column options. `defaultSortDir: 'desc'` on every score-ish column (SteamDB Rating, Wilson Score, Steam %, Metacritic Score, Review Count), ProtonDB, Played (h), and the three "last modified"-style date columns (Last Played, Released, and the Wishlist tab's Added) — a fresh click on any of these now leads with the best/most/most-recent value instead of the worst/least/oldest, which is what a first click almost always means for a column like this. Left the four HLTB columns and the Wishlist tab's own rank column alone: HLTB duration has no obvious "more useful" direction, and wishlist rank is already 1-at-the-top under plain ascending. `defaultValueSort` opens Developer/Publisher/Tags/Categories's filter checklists "most common first" instead of A→Z (higher-cardinality than Genres, which stays alphabetical — it's a small, familiar fixed list) and opens the Last Played/Released/Added date filter trees most-recent-year-first instead of oldest-first. Both are just starting points — the checklist's/tree's own sort-order toggle still cycles through all its states the same as before.

## [0.2.0] - 2026-08-12

### Added

- Accounts bar (both the comparison page and the Library Explorer, shared code in the new `public/accountsBar.js`): one chip per resolved account behind the current search — avatar, persona name (linking to their Steam profile), an online/in-game status dot, a per-account count (games/wishlisted items on the Library Explorer, games on the comparison page), and a "🔒 Private" badge when the profile's visibility isn't public. The private badge closes a real gap: a private profile previously produced a silent empty/failed result indistinguishable from an account that's just genuinely empty. The status dot's tooltip reads "(as of this search)" rather than implying live presence — `personastate`/`gameextrainfo` ride on the same 6h library cache tier as everything else on the chip (see `LIBRARY_CACHE_TTL_MINUTES`), so it's a snapshot, not real-time. On the comparison page, chips are clustered per slot under a "Player N" label once there's more than one slot to tell apart. `POST /api/common-games`'s `slots` and the new `POST /api/wishlist`'s `players` field now carry a `gameCount`/`itemCount` alongside the existing player summary fields to support this — no extra request needed, since the data was already being fetched.
- Each account chip in the accounts bar (both pages) has its own "↻" refresh icon, replacing both pages' old search-bar-level "↻ Refresh" button — lets you force-refresh just one account instead of the whole search. `POST /api/common-games` and `POST /api/wishlist` now accept a `refreshIds` array of already-resolved Steam64 IDs and bypass the cache only for those, via a new `forceIds` option on `getPlayerSummaries` (a single `GetPlayerSummaries` batch call can now bypass the cache for a subset of the requested IDs).
- Both search forms now remember recent searches in the browser's `localStorage` (never sent to the server, namespaced separately per page) and show them as a row of clickable "Recent:" avatar chips above the accounts bar — one click replays that exact search. On the Library Explorer an entry is one Family combo; on the comparison page an entry is the whole set of slots (each possibly itself a multi-account Family), rebuilding every input row on click. Each entry is individually removable, plus a "Clear" for the whole list; capped at the 10 most recent per page.
- The game detail side panel has a "↻" button (next to the game title) that force-refreshes just that game's rating/HLTB/store metadata/tags, bypassing its cache TTL for that one game only. `getCached()` and every Steam/HLTB fetch function in `lib/steam.js`/`lib/hltb.js` accept `{ force: true }` to power this and the per-account refresh above.
- About page (`/about.html`): explains the project, credits its data sources (Steam Web API, SteamSpy, HowLongToBeat), and summarizes what's cached and for how long. Linked from the footer of both the comparison page and Library Explorer, which also links to it now.
- Library Explorer page (`/library.html`): browse a single player's full Steam library in a sortable/filterable/groupable table powered by `@vates/data-table-vanilla`. Shows score, HLTB times, playtime, Metacritic, release date, genres, developers, publishers, tags, and categories. Details stream progressively via SSE as on the main page. Table sort/filter/group/page state is synced to the `?view=` URL param, so a copied link reproduces the same view. Linked from the main page footer.
- Library Explorer: genres, developers, publishers, tags, and categories are multi-value columns — filtering by one of these lists individual values in the checklist instead of matching the whole joined string, and grouping by genre/developer/publisher fans a game out into every group it belongs to.
- Library Explorer: every column except Name is now groupable, and Released is filterable.
- Library Explorer: Score and Metacritic render as bare colored numbers, using the same `scoreColor()` thresholds as the comparison page and side panel; Released now filters through a Year › Month › Day tree instead of a flat date checklist.
- Library Explorer: clicking a row opens the same game detail side panel as the comparison page (hero screenshot/video carousel, score, HLTB, tags, store links), including ↑/↓/🎲 prev/next/random navigation and the ←/→/Esc keyboard shortcuts. The panel rendering itself is shared code (`public/panel.js`); the comparison page's owner list and tag-click filtering are omitted here since this page has neither multi-owner groups nor a table filter to drive. Prev/next/random walk the table's current search/filter/sort order (recomputed via `@vates/data-table-core`'s `searchData`/`processData`), not just insertion order, and cover every matching row rather than only the current page.
- Library Explorer: "Reset view" button (next to the status line, shown once a library is loaded) clears sort/filter/group/page state back to defaults and removes the `?view=` URL param, via the new `resetView()` helper from `@vates/data-table-vanilla`.
- Library Explorer: added the `?` keyboard-shortcuts dialog and `/`-to-focus-input shortcut, matching the comparison page.
- Library Explorer: added a leftmost thumbnail column showing the store capsule image, matching the comparison page. Excluded from sort/filter/group and from full-text search (its column value is forced to `null` so the raw image URL can't match a search query — `@vates/data-table-vanilla`'s `searchData` has no per-column opt-out yet, see [vatesfr/data-table#11](https://github.com/vatesfr/data-table/issues/11)).
- Library Explorer: added a Library/Wishlist tab toggle. The Wishlist tab shows the same table (minus playtime, plus wishlist rank and date added) for the currently loaded player(s)' Steam wishlist, unioned across accounts the same way the Library tab already unions Steam Family libraries. Read-only for now — Steam has no key-based way to mutate a wishlist, only session-authenticated calls as the logged-in user, which this app doesn't support yet. Each tab keeps independent sort/filter/group view state (`?view=` / `?wview=`) and random-pick history; switching tabs auto-fetches for whichever player(s) are loaded.

### Changed

- Library Explorer: Metacritic Score is now hidden by default on both tabs (still available via the column picker), matching the other secondary score columns.
- Game detail side panel: the Metacritic number is now colored using the same `scoreColor()` thresholds as the SteamDB Rating right above it, instead of a flat neutral color.
- Library Explorer: relabeled ambiguous columns for clarity — the existing Score column (Wilson score lower bound) → "Wilson Score", Metacritic → "Metacritic Score", Reviews # → "Review Count" — to make the Steam-user-vs-critic score distinction and the summary-vs-count distinction clear at a glance.
- Library Explorer: added a "SteamDB Rating" column implementing SteamDB's current rating formula (`p − (p − 0.5) × 2^(−log₁₀(n + 1))`, a Bayesian shrinkage toward a neutral 50% prior that fades as review volume grows) — see [steamdb.info-issues#793](https://github.com/SteamDatabase/steamdb.info-issues/issues/793). It's now the **default-visible score column and the table's default sort (descending)** on both tabs, ahead of the more statistically-rigorous but harder-to-explain Wilson Score, which moved to hidden-by-default alongside it.
- Library Explorer: added a "Steam %" column (hidden by default) showing the raw positive/total review ratio — the plain percentage Steam's own store page shows — alongside SteamDB Rating and Wilson Score. No backend change needed for either new column — `event.rating.positive`/`.total` were already flowing through the SSE stream, just unused by the frontend.
- Library Explorer: trimmed the default-visible column set on both tabs from ~12 columns down to the essentials (capsule, name, SteamDB Rating, HLTB All, Played/Wishlist Rank + Added, Released, Genres). Wilson Score, Steam %, Metacritic Score, Main/+Extra/100% HLTB, Developers, and Tags are still available, just hidden by default — toggle them on via the column picker, or "Reset view" restores this new default (including the default sort) anytime.
- Library Explorer: reordered columns so the four score-ish numbers (SteamDB Rating, Wilson Score, Steam %, Metacritic Score) sit contiguously — Metacritic Score used to be stranded after the HLTB/Played block — and moved HLTB's "All (h)" column ahead of Main/+Extra/100%, matching the side panel's "All PlayStyles first" convention instead of trailing behind the specific-playstyle columns.
- `computeSteamdbRating` moved to the shared `public/utils.js` (unit-tested in `test/frontend-utils.test.js`) so the Library Explorer and the game detail side panel compute the same number from one implementation instead of two. It now returns the raw, unrounded value (0-100) rather than a pre-rounded integer, so the Library Explorer's SteamDB Rating sort orders games deterministically by full precision instead of by their display-rounded (and therefore frequently tied) integer. The column is no longer groupable as a result — grouping keys off the raw value, and a near-unique float per game would otherwise split into a useless one-row-per-group table.
- Game detail side panel (shared by the comparison page and Library Explorer): the score section now shows the SteamDB Rating instead of the Wilson score, dropping the Steam review-summary text tier (e.g. "Very Positive") in favor of a compact reviews line, e.g. "99% positive of 465k reviews". Its label reads just "SteamDB" (linking to the game's SteamDB page), matching how "Metacritic" links to its own page right below it.

### Removed

- Library Explorer: removed the "Review Summary" column (Steam's own text tier, e.g. "Very Positive"/"Mixed"). It's computed from Steam's own bucketing thresholds, independent of the SteamDB Rating formula the table defaults to sorting by, so the two could disagree on the same row — the same reasoning that already dropped this text tier from the game detail side panel in favor of a plain reviews count.
- HLTB "All PlayStyles" time (`lib/hltb.js`'s `pickBestMatch` now also extracts `comp_all`) is the new default-displayed HLTB column in the Library Explorer ("All (h)", `hltbAll`) and the first (leftmost) stat in the side panel's How Long To Beat section, ahead of Main Story/+Extra/100% which are still available (hidden by default in the table; always shown in the panel when present). It's the average of every individual completion-time submission pooled together regardless of category — not an average of the other three — so it's a single representative number rather than one specific playstyle.
- Library Explorer: removed the progress-bar rendering (`createScoreBar`) from all four score-ish columns (SteamDB Rating, Wilson Score, Steam %, Metacritic Score) in favor of a bare colored number for every one of them — a bar's fill color carried the same signal the number's color already does, and with up to four such columns visible together the bars added visual weight without adding information. Uses the shared `scoreColor()` from `public/utils.js` (the same function the side panel already uses) instead of a second copy of the color thresholds.

### Fixed

- Test suite: `test/server.test.js` and `test/steam.test.js` now force `DB_FILE=''` (in-memory DB) in-process before requiring `lib/cache`, instead of relying solely on the shell-level `DB_FILE=` prefix in `npm test`'s script. Running either file directly (`node --test test/steam.test.js`, an IDE test runner) previously fell through to `default.env`'s `DB_FILE=db.sqlite` and let the tests' many `_reset()` calls wipe the real, persistent cache.
- Lightbox: during video playback, only the video controls bar auto-hid on idle — the prev/next buttons, toolbar (fullscreen/share/close), and mouse cursor stayed put. The idle timer now toggles a single `lb-idle` class on the lightbox root and CSS hides all of that chrome (plus sets `cursor: none`) together.
- Steam store rate limiting (403): the store semaphore now enforces a 500 ms cooldown per slot after each request completes, capping sustained throughput at ~4 req/s instead of hammering Steam as fast as concurrency allows
- Comparison page: loading the site with no `?u=` param (a fresh visit, or clearing all players) threw `TypeError: Assignment to constant variable` in `loadFromUrl` — a destructured `const slots` shadowed the outer player-slots state variable it was trying to reset
- Library Explorer: `package.json` still pinned `@vates/data-table-core` to `^0.2.0` after `@vates/data-table-vanilla` was bumped to `^0.3.0` (which requires core `^0.3.0`), so the vendored core bundle served under `/vendor/` was missing an export (`isGroupCollapsed`) the vanilla bundle needs — the page's module script threw on load and the table never rendered. Bumped the pin to `^0.3.0`.
- Library Explorer: sorting by a secondary column had no visible effect within groups when grouped by a multi-value column (e.g. group by Tags, sort by Tags then Score) — `@vates/data-table-core` sorted rows globally before fanning them into per-value groups, using each row's full joined tag list as the sort key rather than the group's individual value, so within-group order bore no relation to the secondary sort ([vatesfr/data-table#12](https://github.com/vatesfr/data-table/issues/12)). Bumped `@vates/data-table-core`/`@vates/data-table-vanilla` to 0.4.0, which fixes this upstream.

### Changed

- Cache TTLs raised now that explicit refresh (see Added) covers the "I need this fresher right now" case: library data (owned games/player/wishlist) 60 min → 6 hours; Steam ID resolution 7 days → 90 days; review scores 14 days → 30 days; store metadata/HLTB/tags 30 days → 60 days.
- `@vates/data-table-core` and `@vates/data-table-vanilla` are installed as regular npm dependencies; `server.js` serves their `dist/` folders under `/vendor/`, so the frontend importmap resolves them without a bundler.
- Bumped `@vates/data-table-core`/`@vates/data-table-vanilla` to 0.2.0: filter dropdown redesigned as a searchable master-detail panel with per-value row counts (replacing the old single stacked checklist), column drag-and-drop reordering, and shift-click range selection in filter checklists and the date tree.
- Bumped `@vates/data-table-vanilla` to 0.3.0: rows and group headers are keyboard-navigable (arrows, Home/End, Space to select, Enter to open a row/toggle a group) when `onRowClick` or `selectable` is set.
- Bumped `@vates/data-table-core`/`@vates/data-table-vanilla` to 0.5.0: reworked toolbar layout (grouped actions, dividers, a "Clear all" button), drag-and-drop reordering of active sort entries (mirroring the existing column reordering), an "active sorts"/"active groups" section at the top of the Sort/Group dropdowns, and a clear (×) button inside the search input.
- Extracted the game detail side panel (hero carousel, score/HLTB/tags/links, swipe gestures) from `app.js` into a new shared `public/panel.js`, used by both the comparison page and the Library Explorer. The random-pick "shuffle bag" logic moved there too, generalized to work off any list + queue key instead of just owner groups.
- Docs: clarified that `db.sqlite` is the application database (not purely a cache store) in README, `default.env`, and CLAUDE.md

### Added

- Cache: `npm run cache:clear` wipes all cache entries without deleting the database file, preparing the database for future non-cache tables
- Config: `default.env` (committed to repo) replaces `.env.example` and is loaded automatically as vendor defaults; create `.env` with only the values you want to override — `STEAM_API_KEY` is the only required one
- Config: the server now exits immediately at startup with a clear error message when `STEAM_API_KEY` is missing, instead of warning after the port opens or returning 503 at request time

### Changed

- Cache: renamed default database file from `cache.db` to `db.sqlite` and env var from `CACHE_FILE` to `DB_FILE` to reflect that the database will hold more than just cache data
- Cache: replaced in-memory `Map` + `cache.json` with a SQLite database (`cache.db`) via the built-in `node:sqlite` module; WAL mode enabled; no more debounced JSON flush or process exit hooks — every write is immediately durable; set `CACHE_FILE=` for in-memory mode (used by tests); one table per TTL group (`cache_library`, `cache_resolve`, `cache_rating`, `cache_meta`) so eviction is a single `DELETE … WHERE ts < cutoff` per table and TTL changes take effect on next restart
- Cache: renamed `CACHE_TTL_MINUTES` / `CACHE_TTL_MS` to `LIBRARY_CACHE_TTL_MINUTES` / `LIBRARY_CACHE_TTL_MS` to match the naming pattern of the other TTL env vars
- Cache: split `DETAILS_CACHE_TTL_MINUTES` into three per-prefix TTLs: `RESOLVE_CACHE_TTL_MINUTES` (7 days), `RATING_CACHE_TTL_MINUTES` (14 days), `META_CACHE_TTL_MINUTES` (30 days for store metadata, HLTB, and tags); the longer defaults reduce external API calls for data that rarely changes

### Added

- Keyboard: `/` focuses the first search input from anywhere on the page
- Keyboard: `Enter` on a focused game row opens the game panel
- Keyboard: `Space` (play/pause) and `M` (mute/unmute) documented in the shortcuts modal (were already functional)
- Keyboard: `Enter` on the panel hero image documented in the shortcuts modal (was already functional)
- Game rows now have `tabindex="0"` so they are keyboard-navigable without first opening the panel

- Lightbox: spinner shown while the first image of a newly opened lightbox is loading; cleared on load, error, or close
- Lightbox: `role="dialog"`, `aria-modal="true"`, `aria-label` on the lightbox element; focus moves to the close button on open and returns to the previously focused element on close; Tab/Shift+Tab are trapped within the lightbox while it is open

### Changed

- Refactor: split `getLightbox()` into `createLightboxDom`, `wireButtons`, `wireKeyboard`, `wireMouseHandlers`, `wireTouchHandlers`, `wireVideoControls`
- Refactor: extracted lightbox into `public/lightbox.js` (icons, HLS helpers, zoom/pan, video controls, DOM creation, open/close/step/render); `app.js` shrunk from 1 662 to 1 246 lines; `openLightbox` now takes `game` as an explicit first argument instead of reading the `panelGame` global; `isLightboxOpen()` replaces raw `lightboxShots.length` checks

### Added

- Lightbox: custom video controls replacing the browser's native bar — play/pause button, scrubber with seek, timestamps, mute toggle; controls auto-hide after 3 s of playback inactivity and reappear on mouse move, touch, or pause; `Space` toggles play/pause, `M` toggles mute; arrow keys navigate between media as usual (scrubber focus is handled correctly)
- Lightbox: smooth slide-in transition when navigating between screenshots (new image enters from the direction of travel)
- Keyboard shortcuts modal: press `?` anywhere (outside an input) to open a cheatsheet of all keyboard shortcuts; `Esc` dismisses it; a subtle hint in the page footer points users to it
- Pick Random Game: each group header has a 🎲 button that opens the side panel with a random game from that group (respecting active filters); the side panel nav always shows a 🎲 re-roll button (also `R` key) to pick another game from the same group, avoiding repeats until the whole group has been seen
- Lightbox: screenshots and videos are now directly linkable — the URL gains `?shot=banner|v<id>|s<id>` whenever the lightbox is open and is restored on page load; a share button (🔗) in the lightbox copies the current URL to clipboard
- Lightbox: scroll wheel (or pinch on touch) zooms images up to 4×; drag or single-finger pan when zoomed; zoom resets on navigation
- Lightbox: `F` key toggles fullscreen (mirrors the existing fullscreen button)
- Lightbox: video items now show their thumbnail as a poster frame while the stream initialises

### Fixed

- Lightbox: video now always fills the available viewport in both windowed and fullscreen modes (`object-fit: contain` + explicit `100vw`/`100vh` sizing, matching image behaviour)
- Lightbox: pan is now clamped so the image cannot be dragged completely off-screen when zoomed

### Changed

- Lightbox: media now fills the full viewport (removed `max-width: 96vw` / `max-height: 92vh` margins and rounded corners)
- Lightbox: double-click image to zoom 2× towards the clicked point; double-click again to reset
- Lightbox: adjacent images are preloaded in hidden elements so prev/next navigation is instant
- Lightbox: counter moved to top-centre (was bottom-centre, now overlapped the image at 100vh)
- Lightbox: top-bar controls consolidated into a single `.lb-toolbar` flex row (left: fullscreen + share, centre: counter, right: close) replacing four independently absolute-positioned elements
- Extracted `buildMediaItems`/`resolveShotIndex` into `public/mediaItems.js` and `FILTER_DIMS`/`parseUrlState` into `public/urlState.js`; both modules are covered by unit tests
- Lightbox: images fade in on navigation instead of appearing abruptly
- Side panel filmstrip: items are now `<button>` elements — keyboard-focusable and Tab-navigable, with a visible focus ring
- Side panel: ↑/↓ navigation buttons now roll over (last game wraps to first, and vice versa)

### Fixed

- Side panel: a failed hero image now only hides the main image area, not the whole section including the filmstrip; the filmstrip also no longer stays hidden after navigating to a new game

### Fixed

- Side panel: focus now lands on the hero image when the panel opens, instead of the close button

### Added

- Game table: show a small capsule thumbnail per game, lazy-loaded as rows scroll into view
- Side panel: added IsThereAnyDeal link in the Links section

### Fixed

- Name filter now matches accented/diacritic characters (e.g. typing `o` matches `Ö`)

### Changed

- Lightbox: screenshots now expand to fill up to 96 vw × 92 vh instead of being capped at 1280 px wide

### Added

- Side panel: mousewheel scrolling on the thumbnails filmstrip now scrolls horizontally

### Fixed

- HLTB: search terms now split on punctuation within tokens (e.g. `Bang-On` → `Bang On`, `BIT.TRIP` → `BIT TRIP`) so HLTB's AND matching can find titles it previously missed
- Lightbox: ArrowUp/ArrowDown now navigates to the next/previous game and refreshes the lightbox content (previously the game changed but the lightbox stayed on the old game's media)
- Side panel: ArrowLeft/ArrowRight hero navigation now focuses the hero image so Enter immediately opens the lightbox
- Side panel: filmstrip no longer rebuilds its DOM on hero navigation; only the main image area is updated, preserving scroll position naturally
- Side panel: hero image/video is now focusable (Tab) and opens the lightbox on Enter
- Side panel: focus is now trapped inside the panel while it is open (page content marked `inert`); focus returns to the previously active element on close
- Side panel: ArrowUp/ArrowDown navigation now wraps around (last game → first, first → last)
- Side panel: page now scrolls to keep the selected game row visible when navigating between games
- Side panel and lightbox now show Steam trailer videos (from the store API `movies` field) between the banner and screenshots; filmstrip thumbnails for videos show a ▶ play icon overlay

### Changed

- Side panel: video items in the hero now display as a thumbnail with a ▶ overlay instead of autoplaying; clicking opens the video in the lightbox
- Lightbox: replaced automatic fullscreen on open with an explicit fullscreen toggle button (top-left corner); Escape exits fullscreen if active without closing the lightbox, then closes on a second press

- Side panel: "Owned by" now shows individual Steam accounts as separate chips instead of merging all accounts in a slot into one
- Side panel: left/right arrow keys now scroll through screenshots in the hero carousel (when the lightbox is closed); up/down continue to navigate between games
- Cache now stores raw API responses (Steam reviews, app details, SteamSpy tags, HLTB search results) and applies field extraction at read time; adding new fields from any upstream source no longer requires cache invalidation
- `getGameRating`, `getAppDetails`, and `getSteamSpyTags` now manage their own cache entries (previously handled by `server.js`); `getHLTB` gains an `appid` parameter for the same reason
- Cache schema version bumped to 2 — existing `cache.json` files are discarded automatically on first startup after upgrade (one-time migration)

### Fixed

- `getAppDetails` was missing `metacritic` and `screenshots` fields from the return value when called with an empty data object (pre-existing test bug)
- Steam store API circuit breaker: 2 consecutive 403 responses now block all further store API calls (ratings and metadata) for 5 minutes; a single 403 is still allowed through to avoid false-positives on per-game blocks (removed/region-locked titles)

### Added

- Mobile: lightbox screenshot fills the full viewport (100vw × 100vh) with no padding or rounded corners — swipe navigation replaces the need for margin around the nav buttons
- Lightbox: requests the Fullscreen API on open (hides browser chrome for more screen real estate); exits fullscreen on close and when the user dismisses fullscreen via browser controls
- Side panel: hero image area becomes a carousel — banner first, then screenshots; a thumbnail filmstrip below the hero makes all images immediately visible and clickable; prev/next overlay buttons (visible on hover) remain for sequential navigation; clicking any image in the hero (banner or screenshot) opens the fullscreen lightbox
- Mobile: swipe left/right on the hero image to navigate screenshots; swipe left/right in the lightbox to navigate, swipe down to close
- Side panel: Metacritic score shown below the Wilson score, with a link to the Metacritic page; separated by a divider when both scores are present
- Mobile layout: `@media (max-width: 768px)` breakpoint with reduced container padding, smaller header, tighter card padding, 44px touch targets for remove/nav/close buttons, reduced table column min-widths, halved family-row indent, full-width side panel drawer, wrapped player row (Steam Family button moves below the input line), and 16px input font-size to prevent iOS Safari auto-zoom

### Fixed

- Tags in the side panel now appear in SteamSpy popularity order instead of alphabetically, so the most representative tags appear first

### Added

- Side panel: prev/next navigation buttons (`↑ N / total ↓`) between the header image and the game details; position counter stays accurate when sort or filters change
- Side panel: swipe right to close on touch devices; drag follows the finger with a snap-back animation if the swipe falls short of the threshold

### Added

- "How it works" explainer card shown on initial load (no URL params), with three numbered steps explaining players, the single-vs-multi mode, and Steam Family merging; hidden once a search runs and restored when navigating back to the empty state
- Player slot card: subtitle ("One player: browse their full library. Multiple players: find games everyone owns.") makes the two modes explicit; "+" button now reads "+ Steam Family" with a tooltip describing the merge; a contextual hint appears below the slot when a family member row is added
- Search button label is now dynamic: "Show Library" with one slot, "Find Common Games" with two or more; updates live as players are added or removed

### Changed

- Game names in the results table are no longer links; use the side panel to open the Steam Store page
- Search card heading renamed from "Steam Users" to "Players"

- Game name filter: a text search input at the top of the filter panel lets users narrow results by game title substring; the filter value is persisted in the URL (`?name=…`)
- Single-slot mode: entering one player now shows their full library instead of requiring at least two players
- Fetch top-10 Steam user tags per game from SteamSpy; display them in the side panel and add a Tag filter dimension (filterable/clickable like Genres and Categories)
- Side panel: games with no HLTB match now show a "Search on HowLongToBeat ↗" link instead of a blank section

### Changed

- Split `index.html` into `public/style.css` and `public/app.js` for better caching and editor tooling
- Extracted `renderScoreCell`/`renderMainCell`/`renderExtraCell` helpers in `app.js` to eliminate the three-way cell-rendering duplication; simplified `rowHtml` to a one-liner wrapper over `rowCells`
- Introduced top-level `FILTER_DIMS` constant in `app.js` to replace five separate hardcodings of the filter dimension keys, labels, and URL param names
- `refreshTable` now computes `hasActiveFilters()` once and threads the result through `sortedGames` and `gameMatchesFilters` instead of recomputing it per game
- Moved the player profile link style out of inline JS and into a `.slot-link` CSS class

### Added

- Sort column and direction are now persisted in the URL (`sort=-score`, `sort=name`, etc.) and restored when sharing or navigating back

- Side panel "Owned by" section now shows each account's playtime (e.g. "12h") next to their name; multi-account (Steam Family) slots list each member's playtime individually
- Side panel header image shows a shimmer skeleton while loading to prevent layout shift
- Side panel scrolls back to the top when navigating to a different game (e.g. via arrow keys)

- `POST /api/game-details/stream` endpoint: streams per-game detail events over SSE so the frontend opens one connection per search instead of N individual requests

### Changed

- Frontend loads game details via a single SSE stream (`ReadableStream` + `fetch`) instead of a pool of concurrent `GET /api/game-details/:appid` requests; the old endpoint is unchanged and still used by direct API consumers

### Fixed

- Page no longer freezes while SSE detail events arrive: each event now updates only the affected row's cells instead of re-reconciling every row in every table; sort order and active filters still catch up via a 150 ms debounced `refreshTable`

- `CACHE_FILE` env var controls the cache file path; set to an empty string to disable disk persistence (used by the test script to prevent tests from overwriting `cache.json`)

### Fixed

- Side panel: game description no longer double-encodes HTML entities (e.g. `&quot;` was shown literally instead of as `"`)
- `refreshTable` now reconciles the DOM instead of replacing `tbody.innerHTML`: existing `<tr>` nodes are moved/updated in place, new ones are inserted, and removed ones are discarded. This means row identity is stable across re-renders — clicks, active highlights, and filters all work correctly while details are still loading and the sort order updates live
- Updated tests for `getHLTB` and `getAppDetails` to match current return shapes (`id`, `completionist` for HLTB; `description`, `releaseDate` for app details)

### Added

- Side panel: open state is reflected in the URL as `?game=<appid>`; sharing or reloading the URL restores the panel for that game automatically
- Side panel: ArrowUp/ArrowDown keys navigate to the previous/next game in the current group while the panel is open (skipped when focus is in a text input)
- Side panel: Completionist playtime (HLTB `comp_100`) shown as a third HLTB column when available
- Side panel: release date and short description from the Steam Store API shown below the game title
- Side panel: "Owned by" section listing the player slots that own the selected game
- Side panel: widens to `min(480px, 30vw)` on screens ≥ 1400 px wide
- Side panel: genre, category, developer, and publisher tags are now clickable filter toggles; active filters are highlighted in blue

- Side panel drawer: clicking a game row (outside the Steam link) slides in a detail panel showing the Steam header image, score with review counts, HLTB playtime (with a direct ↗ link to the HowLongToBeat entry), genres, categories, developer, publisher sorted alphabetically, and links to Steam Store / SteamDB / ProtonDB; close with the × button, backdrop click, or Escape

### Fixed

- Side panel no longer stays stuck on skeleton/loading state after game details finish loading: `fetchDetails` now calls `renderPanel()` directly when the loaded game is the currently-open one; filter tag buttons in the panel are no longer replaced mid-click by a concurrent detail load
- HLTB search now normalizes Unicode (e.g. `Ö` → `O`) before building search terms and computing similarity, so games with diacritics in their title (e.g. "Öoo") match the correct HLTB entry instead of a spurious one

- Filters and sort order are now applied immediately as each game's details load: loading games are excluded from active filters (previously shown as matching), and each completed detail fetch triggers a full `refreshTable()` re-sort/re-filter instead of an in-place cell patch

- Filter search inputs and checkboxes no longer lose focus while game details are loading progressively: `updateRow` now updates only the three data cells in-place (score, main, extra) instead of replacing the entire `<tr>`, and `updateFilterOptions` appends new options surgically without rebuilding the filter panel HTML; a full panel rebuild is still done when a new dimension first appears, but focus is saved and restored around it

- Game-details rate limiter no longer counts cache hits. The limit exists to throttle upstream Steam/HLTB calls, but it previously counted every request equally — so refreshing an already-loaded comparison (all cache hits) could exhaust the budget and `429` itself, leaving rows blank. Cache hits now bypass the limiter entirely
- Game-details loading now retries once after a `429`, waiting out the rate-limit window (honoring `Retry-After`), so first-time loads of very large shared libraries recover instead of leaving rows blank
- Game-details loading now checks the HTTP response status before parsing; a `502` error body is no longer stored as game details, which previously left rows rendered blank (no rating/HLTB) instead of falling back gracefully
- Cache loader now distinguishes `ENOENT` (expected on first run) from other errors (JSON parse failure, I/O error); non-ENOENT errors are logged as warnings instead of silently discarded, preventing a corrupted `cache.json` from resetting the cache without any indication
- Cache loader now falls back to `cache.json.tmp` if `cache.json` fails to load, recovering entries from the last in-progress write

### Added

- Filter panel: active filters are shown as removable chips (e.g. "Genre: Action ×") between the header and the dimension columns, making the active set immediately scannable without hunting through checkboxes
- Filter panel: each dimension (Genre, Category, Developer, Publisher) now has a live search box to narrow down long option lists
- Steam store API calls (`getGameRating`, `getAppDetails`) now go through a semaphore (max 2 concurrent) and retry up to twice on 429 responses, with `Retry-After`-aware delay or exponential backoff
- Per-IP rate limits are now configurable via `SEARCH_RATE_LIMIT_MAX` (default 10/min) and `DETAILS_RATE_LIMIT_MAX` (default 300/min)

### Changed

- Frontend detail-loading concurrency reduced from 5 to 2 to reduce the peak number of outbound Steam store requests

- `details:${appid}` cache entry split into three independent entries — `rating:${appid}`, `hltb:${appid}`, `meta:${appid}` — so each source can be cached and retried independently; only fulfilled fetches are cached (rejections are not), so a transient API failure is retried on the next request without discarding successfully-fetched data
- `getCached` now returns `undefined` for a cache miss instead of `null`, allowing `null` to be stored as a valid "no data" result (e.g. a game with no reviews, no HLTB entry, or not on the Steam Store)
- `getGameRating` and `getAppDetails` now throw on non-ok HTTP responses instead of returning `null`, so `Promise.allSettled` can distinguish fetch failures from legitimate "no data" results
- `getHLTB` now throws on auth unavailability, 401/403, and non-ok responses instead of returning `null`; auth is checked before acquiring the concurrency semaphore so a failed auth does not block the queue

- Cache TTL is now resolved from config by key prefix (`getTtlForKey`) rather than being stored in each entry or passed on every read; `getCached` and `setCache` no longer accept a `ttlMs` argument

### Fixed

- Expired cache entries are now evicted on startup; previously they lingered in memory until lazily evicted on first read after a restart
- Removed stale migration guard that re-fetched `details:` cache entries lacking a `meta` field; any such entry has long since expired under the 7-day TTL

- Filter no longer shows games whose Store API metadata fetch failed (null meta): previously `gameMatchesFilters` returned `true` for any falsy meta, conflating "still loading" with "fetch failed"; now only `game.loading === true` bypasses filtering — games with `meta: null` are correctly excluded when any filter is active

- Steam Store API is now requested with `&l=english` so genres and categories are always returned in English instead of the server's IP-geolocated locale
- `/api/game-details` no longer serves cached entries that predate the `meta` field; those entries have `meta: undefined` and are re-fetched transparently so the filter panel populates correctly after upgrading; entries written since the change have `meta: null` (explicit) on Store API failure and are still served from cache normally

- `slotHtml` now validates that `profileurl` starts with `http://` or `https://` before injecting it into an `href` attribute; previously `esc()` did not strip dangerous URL schemes, so a `javascript:` URL would have passed through unchanged and executed on click
- `sortedGames` HLTB sort now uses `?? Infinity` instead of `|| Infinity`; the `||` form incorrectly treated a `0`-hour value as unknown and sorted it to the bottom, because `0` is falsy

### Added

- `normalizeInput`, `scoreColor`, `fmtH`, and `esc` extracted from the inline script into `public/utils.js`, loaded via `<script src>` and testable in Node without a browser (`if (typeof module !== 'undefined') module.exports = ...`); unit tests added in `test/frontend-utils.test.js` (23 new tests, 115 total)
- `getAppDetails(appid)` fetches genres, categories, developers and publishers from the Steam Store API (`store.steampowered.com/api/appdetails`) and adds them as `meta` to the `/api/game-details/:appid` response; data is cached under the same `details:` key (7-day TTL); partial failures are tolerated via `Promise.allSettled` so a rate-limit or timeout on the Store API does not break ratings or HLTB
- Filter panel below the search card lets users narrow results by genre, category, developer and publisher; options populate progressively as game details load; filters are ANDed across dimensions and ORed within each dimension; groups with no matching games are hidden; the results count shows `N / total` when filters are active; games whose metadata has not yet loaded are always shown (not filtered out)
- Filter state is encoded in the URL (`?u=alice&u=bob&genre=Action&cat=Co-op`) so filtered views can be shared and bookmarked; filter params are updated via `replaceState` on every change (fixed dimension order: `genre`, `cat`, `dev`, `pub`; values sorted alphabetically within each); `loadFromUrl` and `popstate` restore the full filter state on page load and back/forward navigation

- `server.js` now exports `{ app }` and guards `app.listen` with `require.main === module` so the app can be imported by tests without binding a port; `STEAM_API_KEY` is now read at request time (not module load) so it can be toggled per-test, and rate limiters are skipped when `NODE_ENV=test`
- TTL constants (`CACHE_TTL_MS`, `DETAILS_CACHE_TTL_MS`) extracted to `lib/config.js`; previously the `DETAILS_CACHE_TTL_MS` formula was copy-pasted in both `steam.js` and `server.js` with two independent default values that could silently diverge
- The `name` query parameter on `/api/game-details/:appid` is now capped at 200 characters before being forwarded to HLTB, preventing oversized strings from inflating outbound request payloads
- `/api/health` now includes `cache: { entries: N }` so it is useful for monitoring the running instance
- Added `morgan('dev')` access logging (skipped in `NODE_ENV=test`) and server-side logging for upstream errors in `/api/common-games` and for rejected `Promise.allSettled` settlements in `/api/game-details`
- `getPlayerSummaries` now caches each player individually under `player:${steamid}` instead of the entire batch under a combined key, so overlapping searches (e.g. [A,B] then [A,B,C]) reuse already-fetched summaries instead of making a redundant API call
- Fixed CLAUDE.md: HLTB similarity threshold was documented as 0.4 but the code uses 0.35 (lowered intentionally to catch edition-suffix mismatches)
- Added `process.on('unhandledRejection')` handler to log and survive promise rejections that escape the try/catch blocks; without it, Node ≥15 crashes the process silently
- Cache is now written atomically: data goes to `cache.json.tmp` first, then renamed over `cache.json`, so a crash mid-write can no longer corrupt the cache file
- `/api/common-games` now returns 502 when the Steam API itself returns an error, and 504 on request timeout, instead of incorrectly returning 400 (Bad Request) for upstream failures; user errors (unknown account, private library) still return 400
- `/api/common-games` now validates that slot values are non-empty strings, returning a clean 400 instead of leaking an internal TypeError when a slot contains `null`, a number, or an empty string
- `getCached` now deletes expired entries on read instead of leaving them in the Map until the next save cycle, freeing memory sooner (especially relevant for large game-library entries)
- Cache save timer is now unref'd so a naturally-exiting process is not held alive for 5 s waiting for the debounce to fire (the `process.on('exit')` handler already flushes synchronously)
- `evictExpired` now uses the TTL stored with each entry instead of a single global max-age, so short-lived `games:` and `players:` entries (60 min) are evicted promptly rather than accumulating for 7 days; `setCache` accepts the TTL as a third argument and all callers have been updated

- Extracted in-flight dedup helper to `lib/dedup.js` (`createDedup`) and replaced the duplicate `_detailsInFlight` block in `server.js` with a call to it, eliminating two independent copies of the same pattern
- HLTB auth init now backs off for 30 s after a failed init request instead of retrying on every queued search
- `getHLTB` now returns null immediately when the game name reduces to an empty string after stripping trademark symbols, avoiding a pointless API round-trip
- HLTB auth thundering herd: concurrent calls that find the token expired now share a single in-flight init request instead of each firing their own
- Steam API cache stampede: concurrent requests for the same vanity URL, library, or player summaries now share a single in-flight fetch instead of each issuing a duplicate API call
- Game-details cache stampede: concurrent requests for the same appid now share a single in-flight fetch (Steam reviews + HLTB) instead of queuing duplicate work through the HLTB concurrency cap

- HLTB lookups now succeed for games with edition suffixes (e.g. "Spiritfarer®: Farewell Edition", "Batman: Arkham Asylum GOTY Edition") — punctuation was stripped from search terms and common edition words ("Edition", "Definitive", "GOTY", etc.) are now excluded to avoid HLTB's strict AND-matching returning empty results
- Lowered HLTB similarity threshold from 0.4 to 0.35 to catch cases where the Steam title includes a subtitle/edition but HLTB only indexes the base title (e.g. "Spiritfarer" vs "Spiritfarer: Farewell Edition", score was 0.379)
- Games with punctuation in their name (colons, em dashes, standalone hyphens) now return HLTB results correctly

### Added

- Integration tests for all route handlers via supertest (`test/server.test.js`): health configured flag, all `POST /api/common-games` validation cases (missing slots, empty slot, null/empty values, too many users, missing API key), happy-path grouping, legacy `users` array, 502 on upstream failure, 400 on private library, `GET /api/game-details` appid validation, cache-hit path, fresh rating+HLTB fetch, and partial failure (null rating) via `Promise.allSettled`
- Unit tests for `createDedup` in `test/dedup.test.js`, covering basic resolution, concurrent-call deduplication, post-resolve retry, post-reject retry, and independent keys
- Unit tests for `resolveSteamId`, `getOwnedGames`, and `getPlayerSummaries` in `test/steam.test.js`, covering Steam64 bypass, API fetch, cache hits, upstream errors, private library, and order-independent cache key
- Unit tests for `cache.js`: TTL checking, eager expiry deletion, entry overwrite, and no-`ttlMs` fallback behaviour (`test/cache.test.js`)
- `TRUST_PROXY` env var to configure Express `trust proxy` (needed for correct rate-limit IPs behind a reverse proxy)
- Steam Family support: click `+` next to any player to add a family member whose library is merged (unioned) into that slot before computing common games
- URL encoding updated to support multi-account slots (`?u=alice,bob_family&u=charlie`); old single-account URLs remain fully compatible
- URL is now canonical: slot members and slots themselves are sorted alphabetically, so the same comparison always produces the same shareable URL

### Fixed

- `package.json`'s `engines.node` claimed `>=18.0.0`, but `lib/cache.js`/`scripts/cache-clear.js` use `node:sqlite`'s `DatabaseSync`, which doesn't exist before Node 22.5.0 and required the `--experimental-sqlite` flag until 22.13.0/23.4.0. Corrected to `>=22.13.0`, the actual minimum that runs this app unflagged.
- Ran `npm audit fix` to pick up patched transitive versions already allowed by existing semver ranges: `body-parser` (via `express`) to 1.20.6, fixing a DoS where an invalid `limit` value silently disabled request-size enforcement; `ip-address` (via `express-rate-limit`) to 10.5.0, fixing SSRF/trust-boundary bypasses from octal/CIDR/IPv4-mapped address misparsing. No `package.json` changes needed — the lockfile just hadn't picked up the fixed patch versions.
- Bumped `express-rate-limit` to `^8.6.2` (from `^8.5.2`), the latest patch release.

## [0.1.0] - 2026-06-16

### Added

- Compare Steam libraries across multiple users by Steam ID, vanity URL, or profile URL
- Group common games by exact set of owners (from most to fewest)
- Wilson score lower bound (95% confidence) from Steam review data
- HowLongToBeat completion times via direct API integration (no npm package)
- Persistent disk cache with separate TTLs for stable vs. frequently-changing data
- URL sharing via `?u=` query params with browser history support
- Progressive loading of ratings and HLTB data (up to 5 concurrent requests)
